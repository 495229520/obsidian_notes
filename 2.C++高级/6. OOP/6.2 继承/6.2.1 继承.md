---
tags:
  - 高级语法
  - OOP/继承
---
# 6.2.1 继承

## 主要问题

1. 继承的本质和原理
2. 派生类的构造过程
3. 重载、覆盖、隐藏
4. 静态绑定和动态绑定
5. 多态、vfptr、vftable
6. 抽象类的设计原理
7. 多重继承以及其问题
8. 虚基类、vbptr、vbtable
9. RTTI
10. C++ 的四种类型强转
11. 继承多态常见笔试

---

## 继承方式与访问限定

**基类成员的访问限定，在派生类里面是不可能超过继承方式的**

| 继承方式          | 基类的访问限定   | 派生类的访问限定    | main 外部的访问限定 |
| ------------- | --------- | ----------- | ------------ |
| **public**    | public    | public      | Yes          |
|               | protected | protected   | No           |
|               | private   | 不可见         | No           |
| **protected** | public    | protected   | No           |
|               | protected | protected   | No           |
|               | private   | 不可见         | No           |
| **private**   | public    | private（可见） | No           |
|               | protected | private（可见） | No           |
|               | private   | 不可见         | No           |

> **多重继承的条件下**也是参照上面的表格，看的是其直接基类的访问限定和自己的继承方式

---

## 总结

1. **外部只能访问对象 public 的成员**，protected 和 private 的成员无法直接访问
2. **在继承结构中**，派生类从基类可以继承过来 private 的成员，但是派生类却无法直接访问
3. **protected 和 private 的区别**：
   - 在基类中定义的成员，想被派生类访问，但是不想被外部访问，那么在基类中把相关成员定义成 protected 保护的
   - 如果派生类和外部都不打算访问，那么在基类中就把相关成员定义成 private 私有的

4. **默认的继承方式**
   - 什么是默认的继承方式：`class B : A`
   - class 定义的派生类，默认继承方式就是 private 私有的
   - struct 定义的派生类，默认继承方式就是 public 共有的

---

## 派生类的构造过程

1. 派生类可以继承来所有的成员（变量和方法），但是**不包括构造函数和析构函数**
2. 派生类如何初始化从基类继承来的成员变量？
   - 通过调用基类相应的构造函数来初始化
3. **派生类从基类继承来的成员的初始化和清理**由基类的构造函数和析构函数负责
4. **派生类对象的构造过程**：
   1. 派生类调用**基类的构造函数**，初始化从基类继承而来的成员
   2. 派生类调用**派生类自己的构造函数**，初始化派生类自己的成员
5. **派生类对象的作用域到期后**，派生类对象的析构过程：
   1. 调用**派生类自己的析构函数**，释放派生类成员可能占用的外部资源（堆内存、文件）
   2. 调用**基类的析构函数**，释放派生类内存中，从基类继承来的成员可能占用的外部资源（堆内存、文件）

---

## 案例

```cpp
class Base
{
public:
    Base(int data = 10) : ma(data)
    {
        cout << "Base()" << endl;
    }
    ~Base()
    {
        cout << "~Base()" << endl;
    }
protected:
    int ma;
};

class Derive : public Base
{
public:
    // Base: 没有合适的默认构造函数可用
    // 从基类继承过来的成员是不允许派生类自己进行初始化的
    /*
    Derive(int data = 20)
        :ma(data), mb(data)
    */
    Derive(int data = 20)
        : Base(data), mb(data)
    {
        cout << "Derive()" << endl;
    }
    ~Derive()
    {
        cout << "~Derive()" << endl;
    }
private:
    int mb;
};

int main()
{
    Derive d(20);
}
```

**输出结果**：
```
Base()
Derive()
~Derive()
~Base()
```

---

## 关键要点

1. **继承不包括构造和析构函数**：派生类必须通过初始化列表调用基类构造函数
2. **构造顺序**：先基类，后派生类
3. **析构顺序**：先派生类，后基类（与构造顺序相反）
4. **访问控制**：派生类中的访问权限不能超过继承方式

---

## 相关链接

- [[12.2.1 类的继承]] - 继承基础
- [[12.2.2 继承构造函数]] - 继承中的构造函数
- [[12.2.3 继承析构函数]] - 继承中的析构函数
- [[6.2.2 重载、隐藏、覆盖]] - 继承中的函数关系
