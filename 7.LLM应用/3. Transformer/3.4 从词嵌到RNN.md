---
tags: LLM
---

### 问题引入
1. 给你几个字让你生成下一个字
2. 给你一句话让你判断句子的褒贬

首先需要将文转换为编码
1. 文字与数字一一对应
	1. 缺点：无法判断词语之间的相关性
	2. 
2. 取一个超级大的向量，每一个词只有向量中的一个值是1，其他都是零（one-hot 独热编码）
	1. 缺点：维度高且稀疏，假如词表中的词很多，那么向量维度会特别高，而且向量之间是相交的，维度太高不好维度太低了也不好，（为什么会这样？）
	2. 用一个维度适合的，这个就是词特征，通过词嵌入的方式得到的
3. 