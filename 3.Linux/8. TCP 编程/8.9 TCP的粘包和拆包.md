---
tags:
  - 平台/linux
  - 网络编程
  - 网络编程/TCP
阅读次数: 0
---

# TCP 的粘包和拆包

> TCP 是基于字节流的协议，没有消息边界，应用层必须自己处理消息的分包与组包问题。

---

## 概念定义

**粘包（Sticky Packet）**：发送方发送的多个独立数据包，在接收方被合并成一个数据包接收。

**拆包（Packet Splitting）**：发送方发送的一个完整数据包，在接收方被拆分成多个数据包接收。

这两种现象的根本原因是 **TCP 是面向字节流的协议**，它不保留消息边界。TCP 只保证数据按顺序、可靠地到达，但不保证每次 `read()` 读取的数据恰好是一个完整的应用层消息。

---

## 为什么会发生

### 1. TCP 的字节流特性

如 [[8.7 IO缓冲]] 所述，TCP 套接字的数据收发是**无边界**的：

```
发送方                              接收方
  │                                   │
  │ send("Hello")                     │
  │ send("World")                     │
  │                                   │
  │ ──────────────────────────────→   │
  │                                   │ recv() → "HelloWorld" (粘包)
```

### 2. 发送端原因

| 原因 | 说明 |
|:---|:---|
| **Nagle 算法** | TCP 为提高网络利用率，会将小数据包合并后再发送 |
| **发送缓冲区** | 多次 `write()` 的数据可能在 [[8.7 IO缓冲#发送缓冲区（Send Buffer）\|发送缓冲区]] 中累积后一起发送 |

### 3. 接收端原因

| 原因 | 说明 |
|:---|:---|
| **接收缓冲区** | 网络数据先存入 [[8.7 IO缓冲#接收缓冲区（Receive Buffer）\|接收缓冲区]]，应用层读取时机不确定 |
| **读取时机** | 应用程序调用 `read()` 时，缓冲区中可能有多个包或不完整的包 |

### 4. 网络传输原因

| 原因 | 说明 |
|:---|:---|
| **MSS 分段** | 数据超过 MSS（Maximum Segment Size）时会被拆分 |
| **MTU 限制** | 数据超过 MTU（Maximum Transmission Unit）时会被 IP 层分片 |

---

## 粘包与拆包示意图

```
┌─────────────────────────────────────────────────────────────────┐
│                         正常情况                                  │
├─────────────────────────────────────────────────────────────────┤
│  发送: [消息A][消息B]                                             │
│  接收: [消息A][消息B]                                             │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                         粘包情况                                  │
├─────────────────────────────────────────────────────────────────┤
│  发送: [消息A][消息B]                                             │
│  接收: [消息A消息B]        ← 两个消息粘在一起                       │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                         拆包情况                                  │
├─────────────────────────────────────────────────────────────────┤
│  发送: [消息A]                                                    │
│  接收: [消息A前半][消息A后半]  ← 一个消息被拆成两次接收               │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                       粘包 + 拆包                                 │
├─────────────────────────────────────────────────────────────────┤
│  发送: [消息A][消息B][消息C]                                       │
│  接收: [消息A消息B前半][消息B后半消息C]  ← 混合情况                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 解决方案

### 方案对比

| 方案 | 优点 | 缺点 | 适用场景 |
|:---|:---|:---|:---|
| **固定长度** | 实现简单 | 浪费空间，不灵活 | 消息长度固定的场景 |
| **分隔符** | 实现简单，可变长度 | 消息内容不能包含分隔符 | 文本协议（如 HTTP） |
| **长度前缀** | 灵活，无限制 | 需要多次读取 | 二进制协议（推荐） |

---

### 方案一：固定长度

每个消息固定为相同长度，不足部分用填充字符补齐。

```cpp
#include <cstring>
#include <sys/socket.h>

constexpr size_t FIXED_MSG_SIZE = 128;

// 发送固定长度消息
ssize_t send_fixed_message(int sockfd, const char* msg) {
    char buffer[FIXED_MSG_SIZE] = {0};
    std::strncpy(buffer, msg, FIXED_MSG_SIZE - 1);
    return send(sockfd, buffer, FIXED_MSG_SIZE, 0);
}

// 接收固定长度消息
ssize_t recv_fixed_message(int sockfd, char* buffer) {
    size_t total_received = 0;
    while (total_received < FIXED_MSG_SIZE) {
        ssize_t n = recv(sockfd, buffer + total_received,
                         FIXED_MSG_SIZE - total_received, 0);
        if (n <= 0) return n;
        total_received += n;
    }
    return total_received;
}
```

---

### 方案二：分隔符

使用特殊字符（如 `\n`、`\r\n`）作为消息边界。HTTP/1.1 协议的头部就使用 `\r\n` 作为分隔符。

```cpp
#include <string>
#include <sys/socket.h>

constexpr char DELIMITER = '\n';
constexpr size_t BUFFER_SIZE = 4096;

// 发送带分隔符的消息
ssize_t send_delimited_message(int sockfd, const std::string& msg) {
    std::string data = msg + DELIMITER;
    return send(sockfd, data.c_str(), data.size(), 0);
}

// 接收带分隔符的消息（需要维护缓冲区状态）
class DelimitedReader {
public:
    // 读取一条完整消息，返回是否成功
    bool read_message(int sockfd, std::string& message) {
        while (true) {
            // 检查缓冲区中是否已有完整消息
            auto pos = buffer_.find(DELIMITER);
            if (pos != std::string::npos) {
                message = buffer_.substr(0, pos);
                buffer_.erase(0, pos + 1);
                return true;
            }

            // 从套接字读取更多数据
            char temp[BUFFER_SIZE];
            ssize_t n = recv(sockfd, temp, sizeof(temp), 0);
            if (n <= 0) return false;
            buffer_.append(temp, n);
        }
    }

private:
    std::string buffer_;
};
```

> [!warning] 注意
> 使用分隔符方案时，必须确保消息内容中不包含分隔符，否则会导致解析错误。对于二进制数据，此方案不适用。

---

### 方案三：长度前缀（推荐）

在每个消息前添加固定长度的头部，标明消息体的长度。这是最通用、最可靠的方案。

```cpp
#include <cstdint>
#include <arpa/inet.h>  // htonl, ntohl
#include <sys/socket.h>
#include <vector>

// 消息头：4 字节长度字段
struct MessageHeader {
    uint32_t length;  // 网络字节序
};

// 发送带长度前缀的消息
ssize_t send_length_prefixed(int sockfd, const void* data, size_t len) {
    // 1. 发送消息头（长度字段，使用网络字节序）
    MessageHeader header;
    header.length = htonl(static_cast<uint32_t>(len));

    if (send(sockfd, &header, sizeof(header), 0) != sizeof(header)) {
        return -1;
    }

    // 2. 发送消息体
    size_t total_sent = 0;
    const char* ptr = static_cast<const char*>(data);
    while (total_sent < len) {
        ssize_t n = send(sockfd, ptr + total_sent, len - total_sent, 0);
        if (n <= 0) return n;
        total_sent += n;
    }

    return total_sent;
}

// 接收带长度前缀的消息
ssize_t recv_length_prefixed(int sockfd, std::vector<char>& buffer) {
    // 1. 接收消息头
    MessageHeader header;
    size_t header_received = 0;
    while (header_received < sizeof(header)) {
        ssize_t n = recv(sockfd, reinterpret_cast<char*>(&header) + header_received,
                         sizeof(header) - header_received, 0);
        if (n <= 0) return n;
        header_received += n;
    }

    // 2. 解析长度（转换为主机字节序）
    uint32_t msg_len = ntohl(header.length);

    // 3. 接收消息体
    buffer.resize(msg_len);
    size_t body_received = 0;
    while (body_received < msg_len) {
        ssize_t n = recv(sockfd, buffer.data() + body_received,
                         msg_len - body_received, 0);
        if (n <= 0) return n;
        body_received += n;
    }

    return msg_len;
}
```

---

## 完整示例：使用长度前缀的回声服务器

### 协议设计

```
┌──────────────────────────────────────────┐
│              消息格式                      │
├────────────────┬─────────────────────────┤
│   4 字节长度    │       消息体             │
│  (网络字节序)   │    (可变长度)            │
└────────────────┴─────────────────────────┘
```

### 服务器端

```cpp
#include <iostream>
#include <cstring>
#include <vector>
#include <sys/socket.h>
#include <netinet/in.h>
#include <unistd.h>
#include <arpa/inet.h>

constexpr int PORT = 8080;
constexpr int BACKLOG = 10;

// 确保读取 n 字节
ssize_t readn(int fd, void* buf, size_t n) {
    size_t nleft = n;
    char* ptr = static_cast<char*>(buf);

    while (nleft > 0) {
        ssize_t nread = read(fd, ptr, nleft);
        if (nread < 0) {
            if (errno == EINTR) continue;
            return -1;
        }
        if (nread == 0) break;  // EOF
        nleft -= nread;
        ptr += nread;
    }
    return n - nleft;
}

// 确保写入 n 字节
ssize_t writen(int fd, const void* buf, size_t n) {
    size_t nleft = n;
    const char* ptr = static_cast<const char*>(buf);

    while (nleft > 0) {
        ssize_t nwritten = write(fd, ptr, nleft);
        if (nwritten <= 0) {
            if (errno == EINTR) continue;
            return -1;
        }
        nleft -= nwritten;
        ptr += nwritten;
    }
    return n;
}

void handle_client(int client_fd) {
    while (true) {
        // 1. 读取 4 字节长度头
        uint32_t net_len;
        if (readn(client_fd, &net_len, sizeof(net_len)) != sizeof(net_len)) {
            break;
        }

        uint32_t msg_len = ntohl(net_len);
        if (msg_len == 0 || msg_len > 1024 * 1024) {  // 限制最大 1MB
            break;
        }

        // 2. 读取消息体
        std::vector<char> buffer(msg_len);
        if (readn(client_fd, buffer.data(), msg_len) != static_cast<ssize_t>(msg_len)) {
            break;
        }

        std::cout << "收到消息: " << std::string(buffer.begin(), buffer.end()) << std::endl;

        // 3. 回声：发送长度头 + 消息体
        if (writen(client_fd, &net_len, sizeof(net_len)) != sizeof(net_len)) {
            break;
        }
        if (writen(client_fd, buffer.data(), msg_len) != static_cast<ssize_t>(msg_len)) {
            break;
        }
    }

    close(client_fd);
}

int main() {
    int server_fd = socket(AF_INET, SOCK_STREAM, 0);

    int opt = 1;
    setsockopt(server_fd, SOL_SOCKET, SO_REUSEADDR, &opt, sizeof(opt));

    sockaddr_in addr{};
    addr.sin_family = AF_INET;
    addr.sin_addr.s_addr = INADDR_ANY;
    addr.sin_port = htons(PORT);

    bind(server_fd, reinterpret_cast<sockaddr*>(&addr), sizeof(addr));
    listen(server_fd, BACKLOG);

    std::cout << "服务器启动，监听端口 " << PORT << std::endl;

    while (true) {
        int client_fd = accept(server_fd, nullptr, nullptr);
        if (client_fd < 0) continue;

        std::cout << "新客户端连接" << std::endl;
        handle_client(client_fd);  // 简单起见，单线程处理
    }

    close(server_fd);
    return 0;
}
```

### 客户端

```cpp
#include <iostream>
#include <string>
#include <vector>
#include <sys/socket.h>
#include <netinet/in.h>
#include <arpa/inet.h>
#include <unistd.h>

constexpr int PORT = 8080;

// readn 和 writen 函数同上...

int main() {
    int sockfd = socket(AF_INET, SOCK_STREAM, 0);

    sockaddr_in addr{};
    addr.sin_family = AF_INET;
    addr.sin_port = htons(PORT);
    inet_pton(AF_INET, "127.0.0.1", &addr.sin_addr);

    if (connect(sockfd, reinterpret_cast<sockaddr*>(&addr), sizeof(addr)) < 0) {
        std::cerr << "连接失败" << std::endl;
        return 1;
    }

    // 发送多条消息测试粘包处理
    std::vector<std::string> messages = {"Hello", "World", "TCP粘包测试"};

    for (const auto& msg : messages) {
        // 发送长度头
        uint32_t net_len = htonl(msg.size());
        writen(sockfd, &net_len, sizeof(net_len));

        // 发送消息体
        writen(sockfd, msg.c_str(), msg.size());

        // 接收回声
        uint32_t recv_len;
        readn(sockfd, &recv_len, sizeof(recv_len));
        recv_len = ntohl(recv_len);

        std::vector<char> buffer(recv_len);
        readn(sockfd, buffer.data(), recv_len);

        std::cout << "收到回声: " << std::string(buffer.begin(), buffer.end()) << std::endl;
    }

    close(sockfd);
    return 0;
}
```

---

## 陷阱与误区

> [!warning] 常见错误

### 1. 假设一次 read 就能读完一条消息

```cpp
// ❌ 错误：可能只读到部分数据
char buffer[1024];
int n = recv(sockfd, buffer, sizeof(buffer), 0);
process_message(buffer, n);  // 消息可能不完整！

// ✅ 正确：循环读取直到收到完整消息
while (total_read < expected_length) {
    int n = recv(sockfd, buffer + total_read, expected_length - total_read, 0);
    if (n <= 0) break;
    total_read += n;
}
```

### 2. 忽略网络字节序

```cpp
// ❌ 错误：直接发送主机字节序
uint32_t len = msg.size();
send(sockfd, &len, sizeof(len), 0);  // 大小端不同的机器会解析错误！

// ✅ 正确：使用网络字节序
uint32_t net_len = htonl(msg.size());
send(sockfd, &net_len, sizeof(net_len), 0);
```

### 3. 未处理 EINTR 错误

```cpp
// ❌ 错误：信号中断时直接返回错误
ssize_t n = read(fd, buf, len);
if (n < 0) return -1;

// ✅ 正确：被信号中断时重试
ssize_t n;
do {
    n = read(fd, buf, len);
} while (n < 0 && errno == EINTR);
```

---

## 禁用 Nagle 算法

在某些低延迟场景下，可以禁用 Nagle 算法来减少粘包：

```cpp
#include <netinet/tcp.h>

int flag = 1;
setsockopt(sockfd, IPPROTO_TCP, TCP_NODELAY, &flag, sizeof(flag));
```

> [!warning] 注意
> 禁用 Nagle 算法只能减少发送端的粘包，不能解决接收端的粘包问题。**应用层协议设计才是根本解决方案。**

---

## 最佳实践

| 推荐 | 避免 |
|:---|:---|
| 使用长度前缀方案 | 依赖 TCP 保持消息边界 |
| 使用 `readn`/`writen` 封装 | 假设一次读写就完成 |
| 使用网络字节序（`htonl`/`ntohl`） | 直接传输主机字节序 |
| 设置合理的消息长度上限 | 无限制地接收数据 |
| 处理 `EINTR` 错误 | 忽略信号中断情况 |

---

## 关键要点

1. **粘包/拆包是 TCP 特性**：TCP 是字节流协议，不保留消息边界
2. **应用层必须处理**：设计应用层协议来识别消息边界
3. **三种方案**：固定长度、分隔符、长度前缀
4. **推荐长度前缀**：最通用、最可靠的方案
5. **循环读写**：永远不要假设一次 `read()`/`write()` 能完成全部数据传输
6. **字节序转换**：跨网络传输整数时必须使用网络字节序

---

## 关联知识

- [[8.7 IO缓冲]] - 理解 TCP 缓冲区是理解粘包的基础
- [[8.8 流控制]] - 流量控制与粘包的关系
- [[8.1 TCP原理]] - TCP 字节流特性
- [[7.5 TCP协议基础]] - TCP 协议基础知识

---

## 参考

- 《Linux多线程服务端编程》第 7 章：muduo 编程示例 - TcpConnection 的消息分包
- 《TCP/IP Illustrated, Volume 1》Chapter 20: TCP Bulk Data Flow

---

#网络/TCP #平台/Linux #跨平台
