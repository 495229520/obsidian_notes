---
tags:
  - 平台/linux
  - 文件操作
阅读次数: 0
---

# 移动文件指针

本节介绍 C 标准库中用于操作[[6.3 读取文件#文件内部指针|文件位置指示器]]的函数，包括 `fseek()`、`ftell()`、`rewind()`、`fgetpos()` 和 `fsetpos()`。

---

## fseek() - 移动文件读写位置

`fseek()` 函数用于移动文件流的读写位置指针。

### 函数原型

```c
#include <stdio.h>

int fseek(FILE *stream, long offset, int fromwhere);
```

### 参数说明

- **stream**：一个指向已打开文件流的指针
- **offset**：一个整数，指定从 fromwhere 开始的偏移量，可以为正、负或 0
- **fromwhere**：指定偏移量的起始位置，可以是以下三个宏之一：

| 宏 | 值 | 说明 |
|----|----|------|
| `SEEK_SET` | 0 | 文件开头 |
| `SEEK_CUR` | 1 | 当前位置 |
| `SEEK_END` | 2 | 文件末尾 |

### 返回值

- 成功：返回 0
- 失败：返回非 0 值，并设置 errno

### fseek 的行为

- `fseek()` 会清除 EOF 指示器。即如果之前读到了文件末尾，`fseek()` 后 `feof()` 会返回 false
- 对于以追加模式（`a` 或 `a+`）打开的文件，`fseek()` **无法**改变写入位置（写入总是在末尾），但可以改变读取位置
- 在文本模式下，offset 的含义可能因平台而异。因此，在文本文件中，通常只建议使用 `fseek(fp, 0, SEEK_SET)` 或 `fseek(fp, ftell_value, SEEK_SET)` 这类操作

### fseek 与 lseek 的对比

`fseek()` 是 C 标准库函数（操作 `FILE*` 流），而 [[4.2 重定向、同步#lseek() - 移动文件偏移量|lseek()]] 是 POSIX 系统调用（操作[[4.1 打开、读取、写入、关闭#文件描述符（File Descriptor）|文件描述符]]）。

| 特性 | fseek() | lseek() |
|------|---------|---------|
| 所属 | C 标准库 | POSIX 系统调用 |
| 操作对象 | FILE* 流 | 文件描述符 fd |
| 返回值 | 0（成功）或非 0（失败） | 新的偏移量或 -1（失败） |

---

## ftell() - 获取当前文件位置

`ftell()` 函数用于获取文件流的当前读写位置。

### 函数原型

```c
#include <stdio.h>

long ftell(FILE *stream);
```

### 参数说明

- **stream**：一个指向已打开文件流的指针

### 返回值

- 成功：返回当前位置（从文件开头算起的字节偏移量）
- 失败：返回 -1L，并设置 errno

### 典型用法

`ftell()` 通常与 `fseek()` 配合使用：

1. 使用 `ftell()` 保存当前位置
2. 进行一些读写操作
3. 使用 `fseek()` 返回到之前保存的位置

---

## rewind() - 重置文件位置到开头

`rewind()` 函数将文件位置指针重新设置到文件的开头。

### 函数原型

```c
#include <stdio.h>

void rewind(FILE *stream);
```

### 参数说明

- **stream**：一个指向已打开文件流的指针

### 返回值

- 无返回值（`void`）

### rewind 的行为

`rewind(fp)` 等价于：

```c
fseek(fp, 0, SEEK_SET);
clearerr(fp);  // 同时清除错误和 EOF 标志
```

`rewind()` 不仅将位置重置到开头，还会清除该文件流的错误标志和 EOF 标志。

---

## fgetpos() 与 fsetpos() - 大文件位置操作

对于非常大的文件（超过 `long` 类型能表示的范围），`ftell()` 和 `fseek()` 可能无法正确工作。`fgetpos()` 和 `fsetpos()` 使用 `fpos_t` 类型来存储位置，可以处理更大的文件。

### 函数原型

```c
#include <stdio.h>

int fgetpos(FILE *stream, fpos_t *pos);
int fsetpos(FILE *stream, const fpos_t *pos);
```

### fgetpos() 参数

- **stream**：一个指向已打开文件流的指针
- **pos**：一个指向 `fpos_t` 变量的指针，用于存储当前位置

### fsetpos() 参数

- **stream**：一个指向已打开文件流的指针
- **pos**：一个指向 `fpos_t` 变量的指针，包含要设置的位置

### 返回值

- 成功：返回 0
- 失败：返回非 0 值，并设置 errno

### fpos_t 类型说明

`fpos_t` 是一个不透明类型（可能是结构体），其内部实现因平台而异。你**不应该**直接操作 `fpos_t` 的值，只能通过 `fgetpos()` 获取、通过 `fsetpos()` 设置。

---

## 综合示例

以下示例演示了 `fseek()`、`ftell()` 和 `rewind()` 的综合用法：

```c
#include <stdio.h>
#include <string.h>

int main() {
    FILE *fp;
    char buffer[100];
    const char *data = "This is a sample text file for fseek and ftell.";

    // 创建测试文件
    fp = fopen("seek_test.txt", "w+");
    if (fp == NULL) {
        perror("fopen");
        return 1;
    }

    // 写入数据
    fputs(data, fp);
    printf("File size: %ld bytes\n", (long)strlen(data));

    // 使用 ftell 获取当前位置（应该在文件末尾）
    long pos = ftell(fp);
    printf("Current file position after write: %ld\n", pos);

    // 使用 rewind 将位置重置到开头
    rewind(fp);
    printf("File position after rewind: %ld\n", ftell(fp));

    // 读取前 10 个字符
    fread(buffer, 1, 10, fp);
    buffer[10] = '\0';
    printf("First 10 chars: '%s'\n", buffer);
    printf("Position after reading 10 bytes: %ld\n", ftell(fp));

    // 使用 fseek 跳到文件中间（位置 20）
    fseek(fp, 20, SEEK_SET);
    printf("Position after fseek(20, SEEK_SET): %ld\n", ftell(fp));

    // 从当前位置再往后跳 5 个字节
    fseek(fp, 5, SEEK_CUR);
    printf("Position after fseek(5, SEEK_CUR): %ld\n", ftell(fp));

    // 跳到文件末尾前 10 个字节
    fseek(fp, -10, SEEK_END);
    printf("Position after fseek(-10, SEEK_END): %ld\n", ftell(fp));

    // 读取最后 10 个字符
    fread(buffer, 1, 10, fp);
    buffer[10] = '\0';
    printf("Last 10 chars: '%s'\n", buffer);

    fclose(fp);
    return 0;
}
```

### 运行输出示例

```
File size: 48 bytes
Current file position after write: 48
File position after rewind: 0
First 10 chars: 'This is a '
Position after reading 10 bytes: 10
Position after fseek(20, SEEK_SET): 20
Position after fseek(5, SEEK_CUR): 25
Position after fseek(-10, SEEK_END): 38
Last 10 chars: 'and ftell.'
```

### 运行分析

1. 文件写入后，`ftell()` 返回 48，表示文件指针在末尾（写入了 48 字节）
2. `rewind()` 将位置重置为 0
3. 读取 10 字节后，位置变为 10
4. `fseek(fp, 20, SEEK_SET)` 将位置设为距文件开头 20 字节处
5. `fseek(fp, 5, SEEK_CUR)` 从当前位置（20）再前进 5 字节，变为 25
6. `fseek(fp, -10, SEEK_END)` 将位置设为距文件末尾（48）倒数 10 字节处，即 38

---

## fgetpos/fsetpos 示例

```c
#include <stdio.h>

int main() {
    FILE *fp;
    fpos_t position;
    char buffer[50];

    fp = fopen("seek_test.txt", "r");
    if (fp == NULL) {
        perror("fopen");
        return 1;
    }

    // 读取一些数据
    fgets(buffer, 20, fp);
    printf("Read: %s\n", buffer);

    // 保存当前位置
    if (fgetpos(fp, &position) != 0) {
        perror("fgetpos");
        fclose(fp);
        return 1;
    }
    printf("Saved file position.\n");

    // 继续读取更多数据
    fgets(buffer, 20, fp);
    printf("Read more: %s\n", buffer);

    // 恢复到之前保存的位置
    if (fsetpos(fp, &position) != 0) {
        perror("fsetpos");
        fclose(fp);
        return 1;
    }
    printf("Restored file position.\n");

    // 再次读取（应该和第二次读取相同）
    fgets(buffer, 20, fp);
    printf("Read again: %s\n", buffer);

    fclose(fp);
    return 0;
}
```

### 运行分析

1. 第一次 `fgets()` 读取文件前 19 个字符
2. `fgetpos()` 保存当前位置
3. 第二次 `fgets()` 继续读取接下来的内容
4. `fsetpos()` 恢复到保存的位置
5. 第三次 `fgets()` 重新读取，输出与第二次相同

---

## 关键要点

- **fseek()**：移动文件位置指针，支持从开头、当前位置或末尾计算偏移
- **ftell()**：获取当前文件位置（字节偏移量）
- **rewind()**：将位置重置到文件开头，并清除错误标志
- **fgetpos()/fsetpos()**：用于大文件，使用 `fpos_t` 类型存储位置

### 函数对比

| 函数 | 功能 | 返回值 | 适用场景 |
|------|------|--------|---------|
| `fseek()` | 移动位置 | 0 或非 0 | 随机访问文件 |
| `ftell()` | 获取位置 | 位置值或 -1 | 查询当前位置 |
| `rewind()` | 重置到开头 | 无 | 重新开始读取 |
| `fgetpos()` | 保存位置 | 0 或非 0 | 大文件、跨平台 |
| `fsetpos()` | 恢复位置 | 0 或非 0 | 大文件、跨平台 |
