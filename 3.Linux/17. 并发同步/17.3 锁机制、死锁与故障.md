---
tags:
  - 平台/linux
  - 并发/同步
  - 并发/锁
阅读次数: 0
---

# 17.3 锁机制、死锁与故障

> 锁是最直接的同步手段：**用串行化换取正确性**。本节先统一对比各类锁，再系统讲解用锁必然带来的三类故障（死锁、活锁、饥饿）及其规避手段。pthread 基础用法详见 [[12.3 互斥量]]。

---

## 一、锁的本质与分类

### 1. 锁的本质

- 把临界区变成"同一时刻只有一个持有者"
- 加锁 = 申请进入，解锁 = 释放

### 2. 锁的分类对比

| 锁类型 | 等待方式 | 适用场景 | pthread / std |
|--------|---------|---------|---------------|
| 互斥锁 (mutex) | 阻塞睡眠 | 临界区较长 | `pthread_mutex_t` / `std::mutex` |
| 自旋锁 (spinlock) | 忙等自旋 | 临界区极短、多核 | `pthread_spinlock_t` |
| 读写锁 (rwlock) | 读共享/写独占 | 读多写少 | `pthread_rwlock_t` / `std::shared_mutex` |
| 递归锁 (recursive) | 同线程可重入 | 递归调用持锁 | `PTHREAD_MUTEX_RECURSIVE` |

### 3. 互斥锁
> 详见 [[12.3 互斥量]]，本节补充其内部实现（futex）

#### futex：用户态快路径 + 内核态慢路径

> TODO: 无竞争时不进内核的原理

### 4. 自旋锁

- 为什么忙等有时反而更快
- 自旋 vs 睡眠的权衡

### 5. 读写锁

- 读者优先 / 写者优先 / 写者饥饿

### 6. 递归锁与它的争议

- 为什么 muduo / Google 不推荐递归锁

### 7. 锁粒度与性能

- 粗粒度 vs 细粒度
- 锁竞争 (contention) 与伪共享 (false sharing)

> TODO: 补充各锁性能对比与选择决策树

---

## 二、用锁的代价：死锁、活锁与饥饿

> 用锁就会有代价。本节系统讲解锁带来的三类典型故障及其规避手段。

### 1. 死锁 (Deadlock)

#### 1.1 死锁的四个必要条件
1. 互斥
2. 持有并等待 (hold and wait)
3. 不可剥夺 (no preemption)
4. 循环等待 (circular wait)

#### 1.2 经典示例：两把锁加锁顺序相反

> TODO: 双线程交叉加锁代码 + 时序图

### 2. 死锁的预防与避免

| 策略 | 做法 |
|------|------|
| 破坏循环等待 | 全局统一加锁顺序 (lock ordering) |
| 破坏持有并等待 | 一次性申请所有锁 / `std::lock` 同时上锁 |
| 破坏不可剥夺 | `trylock` + 回退 |
| 超时 | `pthread_mutex_timedlock` |

### 3. 死锁的检测与调试

- `gdb` 查看线程栈
- 资源分配图 (银行家算法思想)

### 4. 活锁 (Livelock)

- 互相谦让导致谁都进行不下去

### 5. 饥饿 (Starvation) 与公平性

- 写者饥饿、优先级反转 (priority inversion)

> TODO: 补充各故障对比表与最佳实践清单

---

## 相关

- 理论基础：[[17.2 原子性、可见性与内存序]]
- 替代方案：[[17.5 无锁编程：CAS、乐观锁与悲观锁]]
- 策略定位：本章的锁均属**悲观锁** → 见 [[17.5 无锁编程：CAS、乐观锁与悲观锁]]
- C++ 安全加锁：[[17.8 Modern C++ 同步设施与实战并发模式]]（`std::scoped_lock` / `std::lock` 防死锁）
