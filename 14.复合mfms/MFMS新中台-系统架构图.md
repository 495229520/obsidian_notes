---
title: MFMS新中台-系统架构图与数据链路总览
tags:
  - MFMS
  - 架构图
  - 数据中台
  - 系统设计
  - DrawIO
date: 2026-08-21
updated: 2026-08-22
version: v0.4
status: 当前基线
---

# MFMS 新中台 v0.4 · 系统架构图与数据链路总览

> [!abstract] 架构图直览
> 本笔记为 **MFMS 新中台 v0.4 全景架构** 的专属展示与索引文档。核心业务链为“中台需求 → 调度订单 → 调度运单 → Adapter 执行”。

---

## 🖼️ 系统全景架构图（直览）

![[图片/MFMS新中台-架构图.svg]]

> [!tip] 高清矢量图与交互说明
> * 上图为 v0.4 紧凑 SVG 阅读版，支持无损放大；完整可编辑细节保留在 draw.io 图源。
> * 图片物理存储位置：`图片/MFMS新中台-架构图.svg` 与 `图片/SVG/14_1_架构图总览.svg`。

---

## 📂 DrawIO 图源文件与编辑方式

若未来需要修改图表元素或导出其他格式，源文件位置如下：

| 项目 | 路径 / 链接 | 说明 |
| :--- | :--- | :--- |
| **DrawIO 源文件** | `14.复合mfms/MFMS新中台-架构图.drawio` | 包含完整分层网格、组件框、总线泳道与连线的可编辑源文件 |
| **绝对路径** | `/Users/melene/Documents/C++/obsidian_notes-main/14.复合mfms/MFMS新中台-架构图.drawio` | 本地完整绝对路径 |
| **矢量阅读图** | `图片/MFMS新中台-架构图.svg` | 与 DrawIO 同语义的紧凑阅读版 |

### 🛠️ 推荐打开与编辑方式
1. **VS Code 编辑**：安装 `Draw.io Integration` 插件后，在 VS Code 中直接双击 `MFMS新中台-架构图.drawio` 即可直接可视化拖拽编辑并自动保存。
2. **Obsidian 插件**：安装 `Obsidian Diagrams.net (Draw.io)` 插件，直接在 Obsidian 内部点击打开编辑。
3. **本地 Desktop 客户端**：使用 `Draw.io Desktop.app` 直接打开本地 `.drawio` 文件，无需联网拖拽。

---

## 🧭 系统分层与四总线速查（v0.4 基线）

```text
               [ 业务客户端 (MES/Qt/Web) ]              [ 开发者调试客户端 ]
                            │                                     │
                            ▼ (HTTP/WS 请求)                       ▼ (调试控制请求)
┌──────────────────────────────────────────────────────────────────────────────────┐
│              L4 MFMS 数据中台 (C++ 模块化单体 · 局域网小主机)                        │
│   端点与鉴权中心   │   需求与控制应用服务   │   StateManager 状态管理   │   持久化仓储/SDK  │
└──────┬────────────────────┬─────────────────────▲─────────────────────┬──────────┘
       │ 单写 requirement    │ 透传申请/指令         │ 消费事实解码更新      │
═══════╪════════════════════╪═════════════════════╪═════════════════════╪══════════ 4条横向总线泳道
🔵 需求业务通道   MySQL ── mfms_requirement (中台单写)
────────────────────────────────────────────────────────────────────────────────────
🟣 派生业务通道   MySQL ── mfms_order ＆ mfms_waybill (调度单写 · 中台只读)
────────────────────────────────────────────────────────────────────────────────────
🔴 控制调试总线   Cpp-Proxy-SDK / ADK ── QueryControl / Acquire / Force / 调试指令
────────────────────────────────────────────────────────────────────────────────────
🟢 实时事实总线   Redis Stream 统一信封 ── AgvControl / SeerCtrlState / AgvOrderState
═══════╪════════════════════╪═════════════════════╪═════════════════════╪══════════
       │ 读取并拆分需求      │ ADK申请生产控制      │ 发布运单事实          │ 裁决/前置校验
┌──────▼────────────────────▼─────────────────────┴──────────┐          │
│   L3 调度系统 (多实例)  ──>  L2 Adapter (运单与设备适配)    │          │
└────────────────────────────────────────────────────────────┘          ▼
                                                   ┌────────────────────────────────┐
                                                   │ L1 下位机 ｜ 唯一控制权权威与裁决 │
                                                   │ 独占存储 · 原子裁决 · 前置双重校验 │
                                                   └────────────────────────────────┘
```

### 1. 核心分层与组件职责
* **L5/L6 上位应用层**：MES / SCADA / Qt GUI / Web Dashboard / 开发者运维工作台。
* **L4 MFMS 数据中台**：C++ 单进程模块化单体，部署于局域网工控机。职责限于需求校验受理、只读 RequirementView 组装、控制透传与审计、实时流接入及 StateManager 快照维护。
* **L3 调度系统**：读取并接管需求，拆分且单写订单与运单；订单包含站点别名、上下料 type、ids[]、时间戳、数量和优先级。
* **L2 Adapter**：接收调度运单，调 ADK 向下位机申请生产控制权并下发动作，向 Stream 发布可关联运单的进度事实。
* **L1 下位机核心**：**真实控制权唯一权威与终极裁决者**，独占存储持有者，原子裁决 `Query/Acquire/Force/Release`；收到任何动作指令前执行【持有者身份 + 硬件物理安全】双重前置校验；向 Stream 发布物理遥测与 `AgvControl` 快照。
* **L0 物理设备层**：AGV 底盘（SEER / VirtAgv）、机械臂机构（FrRobot / AuboRobot）、力觉传感器、PLC / 夹爪。

---

### 2. 四大横向解耦总线与数据流向
1. 🔵 **【需求业务通道】（MySQL 存储）**：中台 `RequirementIntakeService` 单写 `mfms_requirement`，调度从中读取待处理需求。
2. 🟣 **【订单与运单通道】（MySQL 存储）**：调度系统拆分并单写 `mfms_order` 与 `mfms_waybill`，中台只读组装 `RequirementView`。
3. 🔴 **【控制访问与调试总线】（服务调用）**：中台与 Adapter 向下位机投递申请/指令，下位机独占接收、原子裁决并执行前置双重安全校验。
4. 🟢 **【Redis Stream 实时事实总线】（统一消息信封）**：下位机与 Adapter 发布实时事实，中台 `RedisStreamIngestor` 消费解码更新 `StateManager` 内存快照。

---

## 🚫 四大红线与架构铁律（v0.4）

> [!danger] 红线禁区（中台明确不拥有）
> 1. **严禁在中台实现控制权状态机与锁表**：控制权是下位机物理资产，中台仅为透传者，绝不维护 lease 租约、fencing token、control epoch 或 confirmation token。
> 2. **持久化按职责单写隔离**：中台只写需求，调度只写订单和运单；中台派生表 Repository 必须只读。
> 3. **实时事实单向广播且快照非凭据**：StateManager 快照仅供前端只读展示，过期断流置 `UNKNOWN/STALE` 禁用调试，绝不能作为持权执行凭据。
> 4. **超时属于“结果未知”**：下位机调用超时时，页面必须先通过 `QueryControl` 查询确认当前状态，严禁盲目重复发起强制夺权。

---

## 🔗 相关文档索引
* [[MFMS新中台-架构构造思路]] —— 架构构造五步法、设计准则与历史包袱剖析
* [[MFMS新中台-逐层设计工作台]] —— 各层接口、数据契约、ControlAccess与错误码深度定义
* [[MFMS新中台-Trellis工程结构方案]] —— C++ 模块化单体代码目录与命名空间结构
* [[MFMS新中台-资料索引与权威源]] —— 权威讨论与跨系统对齐记录
