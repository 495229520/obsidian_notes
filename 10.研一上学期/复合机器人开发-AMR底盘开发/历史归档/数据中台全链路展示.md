# 数据中台全链路展示

## 1. 说明

- 本文从 `src/qt_file/src/CommunicationInterface.h` 起，梳理 Qt 前端到数据中台、再到 ROS / DB / 设备适配层的完整调用线路。
- 图中统一约定：
  - `-->` 表示下行请求、调用、状态机推进。
  - `-.->` 表示上行结果、状态回流、Qt 信号回传。
  - `subgraph` 表示线程边界或系统边界。
- 本文只基于源码现状梳理，不改动代码，不推断不存在的链路。

## 2. 总览图

```mermaid
flowchart LR
  classDef ui fill:#eef6ff,stroke:#2563eb,color:#0f172a;
  classDef worker fill:#ecfeff,stroke:#0891b2,color:#083344;
  classDef gateway fill:#f5f3ff,stroke:#7c3aed,color:#3b0764;
  classDef db fill:#fff7ed,stroke:#ea580c,color:#7c2d12;
  classDef ros fill:#f0fdf4,stroke:#16a34a,color:#14532d;
  classDef dev fill:#fdf2f8,stroke:#db2777,color:#831843;
  classDef warn fill:#fff1f2,stroke:#dc2626,color:#991b1b;

  subgraph UI["UI / Main Thread"]
    CI["CommunicationInterface<br/>(只定义信号 + 虚槽)"]
    CII["CommunicationInterfaceImpl<br/>(singleton + lazy init + request* 转发)"]
    HRS["hybrid_robot_system"]
    CTRL["Control"]
  end

  subgraph TH["Communication Thread / QThread"]
    CW["CommunicationWorker<br/>(rclcpp::Node + currentDeviceId + 状态机)"]
  end

  subgraph MID["MFMS Facade"]
    GW["MfmsGatewayImpl"]
    DBS["MfmsDbService"]
    RBS["MfmsRosBridge"]
    CMD["MfmsCommandService"]
  end

  subgraph EXT["DB / ROS / Device Boundary"]
    DBT["MySQL<br/>device_state / device_ui_event / lua_ui_event"]
    TOPIC["ROS Topic<br/>/<id>_state"]
    RPA["RobotProxyAdapter"]
    PROXY["RobotProxy::FrRobot"]
    SVC["ROS Service<br/>/<device_name>_cmd"]
    PUB["FrAdapterPublisher"]
  end

  CI -->|"接口由其实现"| CII
  HRS -->|"刷新设备 / 连接设备"| CII
  CTRL -->|"Jog / 模式切换 / AGV 控制"| CII

  CII -->|"request* 下行"| CW
  CW -->|"门面调用"| GW

  GW --> DBS
  GW --> RBS
  GW --> CMD

  DBS --> DBT
  DBT -.->|"deviceStateChanged / luaStateChanged"| DBS

  RBS -->|"查询设备 / 订阅 Topic"| DBT
  RBS -->|"subscribe / unsubscribe"| TOPIC
  PUB --> TOPIC
  TOPIC -.->|"robotStatusUpdated"| RBS

  CMD --> RPA
  RPA --> PROXY
  PROXY --> SVC

  DBS -.-> GW
  RBS -.-> GW
  CMD -.-> GW
  GW -.-> CW
  CW -.->|"结果 / 状态"| CII
  CII -.->|"Qt signals"| HRS
  CII -.->|"Qt signals"| CTRL

  AGVWARN["AGV 下行已接到 CMD<br/>但 sendAgvMotion()/stopAgv() 当前直接 unsupported"]:::warn
  REFWARN["refreshState() 当前没有真实即时查询<br/>实际等待下一次 Topic 周期推送"]:::warn
  CMD -.-> AGVWARN
  RBS -.-> REFWARN

  class CI,CII,HRS,CTRL ui;
  class CW worker;
  class GW gateway;
  class DBS,DBT db;
  class RBS,TOPIC,PUB ros;
  class CMD,RPA,PROXY,SVC dev;
```

### 总览说明

- `CommunicationInterface` 只是顶层 Qt 接口，真正实现位于 `CommunicationInterfaceImpl`。
- `CommunicationInterfaceImpl` 负责单例、懒初始化、线程切换和 worker 信号转发。
- `CommunicationWorker` 是中台真正的工作入口，独占 `QThread`，内部持有 `rclcpp::Node` 和 `MfmsGatewayImpl`。
- `MfmsGatewayImpl` 是中台门面，向下拆成三条支路：
  - `MfmsDbService`：负责 DB 状态机和事件轮询。
  - `MfmsRosBridge`：负责设备列表查询与 ROS Topic 订阅。
  - `MfmsCommandService`：负责控制命令下发。
- 控制链最终落到 `RobotProxyAdapter -> RobotProxy::FrRobot -> ROS Service`。
- 状态链最终由 `FrAdapterPublisher` 发出 `/<id>_state` Topic，再被 `MfmsRosBridge` 拉回 Qt。

## 3. 场景 1：设备列表链路

```mermaid
flowchart LR
  classDef ui fill:#eef6ff,stroke:#2563eb,color:#0f172a;
  classDef worker fill:#ecfeff,stroke:#0891b2,color:#083344;
  classDef ros fill:#f0fdf4,stroke:#16a34a,color:#14532d;
  classDef db fill:#fff7ed,stroke:#ea580c,color:#7c2d12;

  subgraph UI["UI / Main Thread"]
    UI1["hybrid_robot_system::refreshDeviceList()"]
    IF["CommunicationInterface::refreshRobotList()"]
    IMPL["CommunicationInterfaceImpl::refreshRobotList()"]
    UI2["hybrid_robot_system::onRobotListUpdated()"]
  end

  subgraph TH["Communication Thread"]
    REQ["requestRefreshRobotList"]
    DO["CommunicationWorker::doRefreshRobotList()"]
    CACHE["devices_ 缓存"]
    EMIT["emitRobotList()<br/>append(device.id)"]
  end

  subgraph MID["ROS / DB Boundary"]
    GW["MfmsGatewayImpl::refreshDeviceList()"]
    RB["MfmsRosBridge::refreshDeviceList()"]
    SQL["SELECT ... FROM device_state<br/>WHERE state IN ('offline','load','online','connected')"]
    LIST["deviceListUpdated(QList<OnlineDevice>)"]
    SIG["robotListUpdated -> getRobotList"]
  end

  UI1 --> IMPL
  IF --> IMPL
  IMPL --> REQ --> DO --> GW --> RB --> SQL --> LIST
  LIST -.-> CACHE -.-> EMIT -.-> SIG -.-> UI2

  class UI1,IF,IMPL,UI2 ui;
  class REQ,DO,CACHE,EMIT worker;
  class GW,RB,SIG ros;
  class SQL,LIST db;
```

### 关键说明

- 页面入口在 `hybrid_robot_system::refreshDeviceList()`，最终走到 `CommunicationInterfaceImpl::refreshRobotList()`。
- worker 侧真实动作是 `CommunicationWorker::doRefreshRobotList()`，它通过 `MfmsGatewayImpl` 下发到 `MfmsRosBridge::refreshDeviceList()`。
- `MfmsRosBridge` 会查询 `device_state`，并把 `offline / load / online / connected` 设备都拉出来。
- 查询结果先以 `QList<OnlineDevice>` 形式回到 worker。
- worker 最终回传给 UI 的是 `device.id`，不是 `device.name`，因为 `emitRobotList()` 明确 `append(device.id)`。
- 这意味着 UI 下拉框当前看到的是设备 ID，不是更友好的设备名。

## 4. 场景 2：连接 / 断开状态机

```mermaid
flowchart TD
  classDef ui fill:#eef6ff,stroke:#2563eb,color:#0f172a;
  classDef worker fill:#ecfeff,stroke:#0891b2,color:#083344;
  classDef db fill:#fff7ed,stroke:#ea580c,color:#7c2d12;
  classDef ros fill:#f0fdf4,stroke:#16a34a,color:#14532d;
  classDef dev fill:#fdf2f8,stroke:#db2777,color:#831843;

  UI["hybrid_robot_system::ConnectRobot()"]:::ui
  IF["CommunicationInterface::connectRobot(name)"]:::ui
  IMPL["CommunicationInterfaceImpl::connectRobot(name)"]:::ui
  REQ["requestConnectRobot(name)"]:::worker
  DO["CommunicationWorker::doConnectRobot(name)"]:::worker
  RESOLVE["resolveDeviceId(name)"]:::worker
  STATE{"当前 device_state"}:::db

  LOAD["gateway_->loadDevice(deviceId)"]:::ros
  DBUP["MfmsDbService::loadDevice()<br/>UPDATE device_state='load'"]:::db
  POLL["轮询 device_ui_event"]:::db
  EVT["deviceStateChanged(..., toState)"]:::db

  CONN["gateway_->connectRobot(deviceId)"]:::ros
  CMD["MfmsCommandService::connectRobot()"]:::dev
  RPA["RobotProxyAdapter::connectRobot()"]:::dev
  PROXY["RobotProxy::FrRobot::connect()"]:::dev
  OK["robotConnected(success)"]:::dev

  SUB["gateway_->subscribeDeviceById(deviceId)"]:::ros
  RBS["MfmsRosBridge::subscribeDeviceById()"]:::ros
  ROSSUB["create_subscription(...)"]:::ros
  SUBRES["subscribeResult(success)"]:::ros
  RET["connectionResult(true/false)"]:::ui

  DISC["CommunicationInterface::disconnectRobot() / Impl"]:::ui
  DREQ["requestDisconnectRobot"]:::worker
  DDO["CommunicationWorker::doDisconnectRobot()"]:::worker
  UNSUB["gateway_->unsubscribe()"]:::ros
  DCONN["gateway_->disconnectRobot(currentDeviceId_)"]:::ros
  CLEAR["clear currentDeviceId_<br/>clear connectingDeviceId_"]:::worker

  UI --> IMPL
  IF --> IMPL
  IMPL --> REQ --> DO --> RESOLVE --> STATE

  STATE -- connected --> CONN --> CMD --> RPA --> PROXY --> OK
  STATE -- online --> EVT
  STATE -- offline / load --> LOAD --> DBUP --> POLL --> EVT
  EVT -- toState = connected --> CONN
  EVT -- toState = offline / unload --> RET

  OK --> SUB --> RBS --> ROSSUB --> SUBRES -.-> RET

  DISC --> DREQ --> DDO --> UNSUB --> DCONN --> CLEAR

  RET -.-> UI
```

### 关键说明

- `connectRobot(name)` 进入 worker 后，不会直接连设备，而是先 `resolveDeviceId(name)`。
- 然后按 DB 里的当前设备状态分三路：
  - `connected`：直接进入 `gateway_->connectRobot(deviceId)`。
  - `online`：先等待 DB 状态继续推进到 `connected`，再发起真正连接。
  - `offline / load`：先 `loadDevice()`，通过 DB 状态机推进。
- `offline / load` 分支会走 `MfmsDbService::loadDevice()`，更新 `device_state`，再靠轮询 `device_ui_event` 收到 `deviceStateChanged`，进入 `connected` 后再继续连接。
- 连接成功不是一步结束，而是两阶段：
  - 第一阶段：`RobotProxyAdapter::connectRobot()` 成功。
  - 第二阶段：继续 `subscribeDeviceById(deviceId)`，订阅成功后才回 `connectionResult(true)`。
- 断开时会先 `unsubscribe()`，再 `disconnectRobot(currentDeviceId_)`，最后 worker 清空 `currentDeviceId_` 和 `connectingDeviceId_`。

## 5. 场景 3：控制命令链路

```mermaid
flowchart TB
  classDef ui fill:#eef6ff,stroke:#2563eb,color:#0f172a;
  classDef worker fill:#ecfeff,stroke:#0891b2,color:#083344;
  classDef dev fill:#fdf2f8,stroke:#db2777,color:#831843;
  classDef warn fill:#fff1f2,stroke:#dc2626,color:#991b1b;

  CTRL["Control 页面信号<br/>sendJogJoint / sendJogCartesian / sendARMMode / sendAGVMove*"]:::ui
  IMPL["CommunicationInterfaceImpl override slots"]:::ui
  W["CommunicationWorker"]:::worker

  JOINT["关节 Jog<br/>doArmJogJoint()<br/>-> gateway_->jogRobotAxis()<br/>-> MfmsCommandService::jogRobotAxis()<br/>-> RobotProxyAdapter::moveAxid()"]:::dev
  CART["笛卡尔 Jog<br/>doArmJogCartesian()<br/>-> jogRobotCartesian()<br/>-> getDescPose()<br/>-> 计算 targetPos<br/>-> moveL()"]:::dev
  MODE["模式切换<br/>doArmChangeMode()<br/>-> setRobotMode()<br/>-> RobotProxyAdapter::setMode()<br/>-> 设备侧 SETMODE"]:::dev
  AGV["AGV 控制<br/>doAgvMove*()<br/>-> sendAgvMotion()<br/>-> 当前直接 unsupported"]:::warn

  DEV["RobotProxy::FrRobot / ROS Service<br/>MOVEAXIS / MOVEL / SETMODE"]:::dev
  RET["robotMotionExecuted / robotModeExecuted / agvMotionExecuted"]:::worker
  UIRET["Control::armControlRes / agvControlRes / armChangeModeRes"]:::ui

  CTRL --> IMPL --> W
  W --> JOINT --> DEV
  W --> CART --> DEV
  W --> MODE --> DEV
  W --> AGV
  JOINT -.-> RET
  CART -.-> RET
  MODE -.-> RET
  AGV -.-> RET
  RET -.-> W -.-> IMPL -.-> UIRET
```

### 关键说明

- `Control` 页面已经把 ARM jog、AGV 前后左右、模式切换、结果回调全部接到 `CommunicationInterfaceImpl`。
- ARM 关节 jog 是完整闭环：
  - `armJogJoint`
  - `requestArmJogJoint`
  - `doArmJogJoint`
  - `jogRobotAxis(deviceId, jointNum + 1, dir, step)`
  - `RobotProxyAdapter::moveAxid()`
  - 设备服务
- ARM 笛卡尔 jog 不是直接一步走到底，它会先 `getDescPose()` 获取当前位置，再计算目标位姿，然后调用 `moveL()`。
- ARM 模式切换链路是：
  - `armChangeMode`
  - `doArmChangeMode`
  - `setRobotMode()`
  - `RobotProxyAdapter::setMode()`
  - 设备服务 `SETMODE`
- AGV 控制链路在 Qt / Worker / Gateway 层都已接好，但 `MfmsCommandService::sendAgvMotion()` / `stopAgv()` 当前直接返回 unsupported，不是完整闭环。
- 所有控制结果都统一回流为 `robotMotionExecuted` / `robotModeExecuted` / `agvMotionExecuted`，再经 worker 和 `CommunicationInterfaceImpl` 回到页面槽函数。

## 6. 场景 4：状态回流链路

```mermaid
flowchart LR
  classDef ros fill:#f0fdf4,stroke:#16a34a,color:#14532d;
  classDef worker fill:#ecfeff,stroke:#0891b2,color:#083344;
  classDef ui fill:#eef6ff,stroke:#2563eb,color:#0f172a;
  classDef warn fill:#fff1f2,stroke:#dc2626,color:#991b1b;

  subgraph SUB["Subscription Setup"]
    TNAME["OnlineDevice::topicName()<br/>= /<id>_state"]:::ros
    BYTYPE["MfmsRosBridge::subscribeByType()"]:::ros
    FRSUB["FrRobotState 订阅"]:::ros
    HSSUB["HsRobotState 订阅"]:::ros
    AGVSUB["SeerAgvState 订阅"]:::ros
  end

  subgraph ROSCB["ROS Callback / Convert"]
    ONFR["onFrRobotState()<br/>-> convertFrMessage()"]:::ros
    ONHS["onHsRobotState()<br/>-> convertHsMessage()"]:::ros
    ONAGV["onSeerAgvState()<br/>-> RobotRealtimeStatus"]:::ros
    RT["robotStatusUpdated(RobotRealtimeStatus)"]:::ros
  end

  subgraph TH["Communication Thread"]
    HANDLE["CommunicationWorker::handleRobotStatus()"]:::worker
    ARMMSG["构造 FrRobotState::SharedPtr"]:::worker
    AGVMSG["构造 SeerAgvState::SharedPtr"]:::worker
    REF["refreshState()<br/>-> doRequestState()<br/>-> MfmsGatewayImpl::requestState()<br/>只写日志，等待下一次 Topic"]:::warn
  end

  subgraph UI["UI Return"]
    ARMSIG["sendARMState"]:::ui
    AGVSIG["sendAGVState"]:::ui
    HRS["hybrid_robot_system::onArmStateReceived / onAgvStateReceived"]:::ui
    CTRL["Control::getARMState / getAGVState"]:::ui
  end

  TNAME --> BYTYPE
  BYTYPE --> FRSUB --> ONFR --> RT
  BYTYPE --> HSSUB --> ONHS --> RT
  BYTYPE --> AGVSUB --> ONAGV --> RT

  RT -.-> HANDLE
  HANDLE -- 非 AGV --> ARMMSG -.-> ARMSIG
  HANDLE -- AGV --> AGVMSG -.-> AGVSIG

  ARMSIG -.-> HRS
  ARMSIG -.-> CTRL
  AGVSIG -.-> HRS
  AGVSIG -.-> CTRL

  REF -.-> RT
```

### 关键说明

- 状态订阅目标名来自 `OnlineDevice::topicName()`，规则是 `/<id>_state`。
- `MfmsRosBridge::subscribeByType()` 会按设备类型创建三类订阅：
  - `FrRobotState`
  - `HsRobotState`
  - `SeerAgvState`
- ROS 回调统一先转换成 `RobotRealtimeStatus`。
- 然后由 `CommunicationWorker::handleRobotStatus()` 再分流成：
  - ARM：构造 `FrRobotState::SharedPtr`，发 `sendARMState`
  - AGV：构造 `SeerAgvState::SharedPtr`，发 `sendAGVState`
- UI 侧主要接收方有两个：
  - `hybrid_robot_system`
  - `Control`
- `refreshState()` 目前没有真正向下游发起即时查询；`MfmsGatewayImpl::requestState()` 只有日志说明，实际依赖下一次周期性 Topic 推送。

## 7. Topic / Service 命名边界

### Topic 命名

- 中台侧订阅名来自 `OnlineDevice::topicName()`：

```cpp
QString topicName() const {
    const QString topicBase = id.isEmpty() ? name : id;
    return QString("/%1_state").arg(topicBase);
}
```

- 因此本文统一按 `/<id>_state` 展示状态 Topic。

### Service 命名

- 设备服务边界本文统一展示为 `/<device_name>_cmd`。
- Fr 适配器源码中当前默认实现是：

```cpp
service_name_ = node_name_ + "_cmd";
```

- 所以在 Fr 默认实例下，更接近 `/FrAdapterServer_cmd` 这种实际命名。
- 如果部署侧要求严格满足 `/<device_name>_cmd`，则 DB 中设备 ID、适配器节点名、代理创建名三者需要一致。

## 8. 三个现状风险

### 风险 1：AGV 控制未真正落地

- `CommunicationInterfaceImpl`
- `CommunicationWorker`
- `MfmsGatewayImpl`

这三层已经把 AGV 下行接口接起来了，但 `MfmsCommandService::sendAgvMotion()` / `stopAgv()` 当前直接返回 unsupported，所以不是完整闭环。

### 风险 2：`refreshState()` 不是实时查询

- `refreshState()` 会一路调用到 `CommunicationWorker::doRequestState()`。
- 但 `MfmsGatewayImpl::requestState()` 目前只有日志，没有真实下钻查询动作。
- 结果上看，它不是“立刻取一次最新状态”，而是“等待下一次周期性 Topic 推送”。

### 风险 3：设备列表 UI 显示的是 ID 而不是 Name

- `MfmsRosBridge` 查询时其实能拿到 `name` 和 `id`。
- 但 `CommunicationWorker::emitRobotList()` 最终回传的是 `device.id`。
- 所以 UI 下拉框当前展示的是设备 ID，这会影响操作体验，也容易让“设备名”和“部署标识”混淆。

## 9. 结论

- 从顶层 `CommunicationInterface.h` 往下看，数据中台的主干非常清晰：
  - Qt 接口层
  - 单例线程桥接层
  - Worker 执行层
  - Gateway 门面层
  - DB / ROS / 命令三支路
  - 设备适配层
- 真正最关键的两条闭环是：
  - 控制闭环：`Qt -> Worker -> Gateway -> CommandService -> RobotProxyAdapter -> Device`
  - 状态闭环：`Device / Publisher -> ROS Topic -> RosBridge -> Worker -> Qt`
- 当前最需要注意的三个缺口是：
  - AGV 控制尚未落地
  - `refreshState()` 不是即时查询
  - 设备列表对外展示的是 `id`

