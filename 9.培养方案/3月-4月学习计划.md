# 3 月 – 4 月每日学习计划

> **时间范围**：2026-03-09 ~ 2026-04-30（共 53 天）
> **对应总体方案**：Phase 1 加速版（第 1-3 月压缩到 2 个月）
> **前提**：你已有 C++ 基础 100 篇笔记、Linux 系统编程 75 篇笔记、远控系统项目经验。不需要从零学基础。

---

## 总体目标

到 4 月 30 日，你应该完成：

1. ✅ `cpp-core-lab` 仓库 — 包含 CMake 工程化 + sanitizer + spdlog + 6 个核心实验
2. ✅ `cpp-threadpool` — 生产级线程池，带 benchmark
3. ✅ `cpp-service-core` v1 — 分层服务端框架雏形，能跑、能测、能讲
4. ✅ 至少 3 篇技术总结文档
5. ✅ 能口头讲清楚：请求从 socket 进来到响应出去的完整生命周期

---

## 每日时间建议

| 类型 | 工作日 | 周末 |
|------|--------|------|
| 核心学习/编码 | 2-3 小时 | 4-5 小时 |
| 阅读/画图/笔记 | 0.5 小时 | 1 小时 |

> 如果某天确实没时间，至少做 30 分钟阅读，保持连续性。

---

## 第 1 周：工程化启动（3/9 – 3/15）

> 核心任务：建仓库、配工具链、把已有知识转化成工程化代码

### 3/9（周一）

- [ ] 在 GitHub 建仓库 `agent-infra-roadmap`（总仓库，放 README 路线图）
- [ ] 建仓库 `cpp-core-lab`
- [ ] 初始化 CMake 项目结构：
  ```
  cpp-core-lab/
  ├── CMakeLists.txt
  ├── src/
  ├── include/
  ├── tests/
  └── experiments/
  ```
- [ ] 安装 vcpkg，配置 CMake toolchain file
- **产出**：两个仓库能 `cmake --build` 通过

### 3/10（周二）

- [ ] 通过 vcpkg 安装 spdlog、nlohmann-json、googletest
- [ ] 在 `cpp-core-lab` 里写一个最小 spdlog demo（info/warn/error/文件输出）
- [ ] 配置 AddressSanitizer（CMake 里加 `-fsanitize=address`）
- [ ] 写一个故意 use-after-free 的例子，验证 ASan 能报出来
- **产出**：工具链验证通过

### 3/11（周三）

- [ ] 实验 1：`shared_ptr` / `weak_ptr` 循环引用
  - 构造循环引用，用 ASan 检测泄漏
  - 用 `weak_ptr` 打破，验证析构正确调用
- [ ] 实验 2：`unique_ptr` 资源托管
  - RAII 管理文件句柄
  - 自定义 deleter
- **产出**：`experiments/smart_ptr/` 目录，带注释

### 3/12（周四）

- [ ] 实验 3：move vs copy 行为对比
  - 实现一个带移动构造的 `Buffer` 类
  - 用 `std::move` 转移所有权，打印构造/析构时机
  - 验证移动后源对象状态
- [ ] 实验 4：Rule of 5 完整实现
  - 一个类同时实现拷贝构造、拷贝赋值、移动构造、移动赋值、析构
  - 每个函数都打印日志
- **产出**：`experiments/move_semantics/`

### 3/13（周五）

- [ ] 实验 5：`std::function` + lambda 回调
  - 写一个简单的事件注册器：`register_handler(event, callback)`
  - 用 lambda 捕获不同变量测试
- [ ] 实验 6：线程安全容器
  - 用 `mutex` 包装一个线程安全队列
  - 多线程 push/pop 压力测试
- **产出**：`experiments/callback/` 和 `experiments/thread_safe_queue/`

### 3/14（周六）

- [ ] 配置 ThreadSanitizer（`-fsanitize=thread`）
- [ ] 在线程安全队列实验中，故意不加锁，让 TSan 报 data race
- [ ] 修复并验证
- [ ] 给 `cpp-core-lab` 写 README：列出所有实验、每个实验学到什么
- **产出**：README v1

### 3/15（周日）

- [ ] 复盘本周：哪些知识点确认掌握，哪些有新发现
- [ ] 更新 Obsidian 笔记：把 sanitizer 使用方法补到相关笔记中
- [ ] 规划下周任务
- **产出**：周复盘笔记

---

## 第 2 周：线程池（3/16 – 3/22）

> 核心任务：写一个可用于后续项目的线程池

### 3/16（周一）

- [ ] 建仓库 `cpp-threadpool`
- [ ] 设计线程池接口：
  ```cpp
  class ThreadPool {
  public:
      explicit ThreadPool(size_t threads);
      ~ThreadPool();
      template<class F, class... Args>
      auto submit(F&& f, Args&&... args) -> std::future<...>;
  };
  ```
- [ ] 阅读参考：《Linux 多线程服务端编程》第 3 章 线程同步
- **产出**：接口设计文档（注释即可）

### 3/17（周二）

- [ ] 实现 v1 线程池核心：
  - 任务队列（`std::queue` + `mutex` + `condition_variable`）
  - worker 线程循环
  - `submit()` 返回 `std::future`
- **产出**：能编译通过的 v1

### 3/18（周三）

- [ ] 写测试：
  - 基本功能：提交 100 个任务，验证全部执行
  - 返回值：验证 `future.get()` 拿到正确结果
  - 异常：任务抛异常时 `future.get()` 能捕获
- [ ] 用 TSan 跑测试，确认无 data race
- **产出**：`tests/` 通过

### 3/19（周四）

- [ ] 优化线程池：
  - 优雅关闭（析构时等待所有任务完成）
  - 支持任务优先级（`std::priority_queue`，可选）
- [ ] 写 benchmark：
  - 不同线程数 vs 任务数下的吞吐量
  - 空任务 vs 计算密集型 vs IO 密集型对比
- **产出**：benchmark 结果记录

### 3/20（周五）

- [ ] 将线程池封装成可复用的库（单独的 CMake target）
- [ ] 确保后续 `cpp-service-core` 能通过 `add_subdirectory` 或 `find_package` 引入
- **产出**：可复用的线程池库

### 3/21（周六）

- [ ] 阅读 muduo 的 `EventLoopThreadPool` 实现（源码在 `书籍/Linux多线程服务端编程.pdf` 对应的 GitHub 仓库）
- [ ] 对比自己的实现，记录差异
- [ ] 写 README：
  - 设计决策
  - 使用方法
  - benchmark 数据
- **产出**：README 完成

### 3/22（周日）

- [ ] 复盘：线程池中遇到的并发问题（死锁、条件变量误用等）
- [ ] 更新 Obsidian 笔记：把线程池设计模式补充到线程相关笔记
- [ ] 总结文档：《一个 C++ 线程池的设计与实现》
- **产出**：第 1 篇技术总结

---

## 第 3 周：epoll reactor 重构（3/23 – 3/29）

> 核心任务：把你已有的 epoll 知识升级成 reactor 模式

### 3/23（周一）

- [ ] 复习自己的 epoll 笔记（`3.Linux/13.IO复用/`）
- [ ] 阅读《Linux 多线程服务端编程》第 6 章：muduo 网络库设计
- [ ] 画出 Reactor 模式核心类图（Mermaid）：
  - EventLoop
  - Channel
  - Poller (epoll wrapper)
  - Acceptor
- **产出**：Reactor 类图

### 3/24（周二）

- [ ] 建目录 `cpp-core-lab/reactor/`
- [ ] 实现 `Channel` 类：
  - 封装 fd + 感兴趣的事件 + 回调函数
  - `handleEvent()` 根据 revents 分发
- [ ] 实现 `Poller` 类：
  - 封装 `epoll_create`, `epoll_ctl`, `epoll_wait`
  - 返回活跃的 Channel 列表
- **产出**：Channel + Poller 编译通过

### 3/25（周三）

- [ ] 实现 `EventLoop` 类：
  - `loop()` 主循环：调 Poller → 得到活跃 Channel → 逐个 handleEvent
  - `quit()` 优雅退出
  - `runInLoop()` / `queueInLoop()`：跨线程投递回调
- **产出**：EventLoop 能跑起来

### 3/26（周四）

- [ ] 实现 `Acceptor` 类：
  - 封装 listen socket
  - 新连接到来时回调
- [ ] 组装一个最小 echo server：
  - Acceptor 接受连接
  - 注册读事件
  - echo 回去
- **产出**：reactor echo server 能跑通

### 3/27（周五）

- [ ] 接入之前的线程池：one loop per thread 模式
  - 主线程跑 Acceptor
  - IO 线程各自跑一个 EventLoop
- [ ] 写简单 benchmark：对比单线程 reactor vs 多线程 reactor
- **产出**：多线程 reactor

### 3/28（周六）

- [ ] 加 Buffer 类：处理粘包拆包
  - 参考 muduo 的 Buffer 设计
  - 支持 prependable / readable / writable 三段式
- [ ] 测试：大量小包 / 大包场景
- **产出**：带 Buffer 的完整 reactor

### 3/29（周日）

- [ ] 复盘本周
- [ ] 画完整的 reactor 架构图（更新 Mermaid）
- [ ] 更新 Obsidian epoll 笔记：补充 reactor 模式内容
- [ ] 总结文档：《从 epoll 到 Reactor：一步步搭建事件驱动框架》
- **产出**：第 2 篇技术总结

---

## 第 4 周：服务框架雏形（3/30 – 4/5）

> 核心任务：开始 `cpp-service-core`，把前面的积累整合成分层服务框架

### 3/30（周一）

- [ ] 建仓库 `cpp-service-core`
- [ ] 设计分层架构：
  ```
  cpp-service-core/
  ├── src/
  │   ├── net/          # reactor + connection
  │   ├── protocol/     # 消息编解码
  │   ├── dispatch/     # 请求分发
  │   ├── worker/       # 业务处理
  │   ├── config/       # 配置加载 (yaml-cpp)
  │   ├── log/          # spdlog 封装
  │   └── metrics/      # 基础指标
  ├── include/
  ├── tests/
  └── examples/
  ```
- [ ] 把 reactor 代码迁移到 `net/` 模块
- **产出**：仓库结构建立，reactor 迁移完成

### 3/31（周二）

- [ ] 定义消息协议：
  ```
  [4B length][2B type][payload]
  ```
- [ ] 实现 `Codec` 类：编码/解码
- [ ] 实现 `TcpConnection` 类：
  - 管理单个连接的读写
  - 集成 Buffer 和 Codec
- **产出**：protocol 层完成

### 4/1（周三）

- [ ] 实现 `Dispatcher` 类：
  - 注册 handler：`registerHandler(msgType, callback)`
  - 收到消息后按 type 分发
- [ ] 实现最小 `TcpServer` 类：
  - 管理所有 TcpConnection
  - 接受连接 → 创建 Connection → 绑定 Dispatcher
- **产出**：消息分发链路跑通

### 4/2（周四）

- [ ] 接入 spdlog：
  - 定义日志宏（`LOG_INFO`, `LOG_ERROR` 等）
  - 配置文件输出 + 控制台输出
  - 关键路径加日志：连接建立/断开、消息收发、错误
- [ ] 接入 yaml-cpp：
  - 配置文件：端口、线程数、日志级别
  - `Config` 单例加载
- **产出**：日志 + 配置集成

### 4/3（周五）

- [ ] 接入线程池（从 `cpp-threadpool` 引入）
- [ ] 实现 `WorkerPool`：
  - Dispatcher 把任务提交到 WorkerPool
  - Worker 处理完回调到 IO 线程发响应
- [ ] 测试完整链路：client 发请求 → reactor 收 → 解码 → 分发 → worker 处理 → 编码 → 发回
- **产出**：完整请求链路跑通

### 4/4（周六）

- [ ] 写 `examples/echo_server.cpp`：基于框架的 echo server
- [ ] 写 `examples/calculator_server.cpp`：简单计算服务（加减乘除），演示消息分发
- [ ] 写测试客户端
- [ ] 压测：1000 个并发连接、10000 个请求
- **产出**：两个示例 + 压测数据

### 4/5（周日）

- [ ] 复盘本周
- [ ] 画 `cpp-service-core` 架构图（Mermaid）
- [ ] 写 README v1：项目介绍、架构图、使用方法、示例
- [ ] 更新 Obsidian 笔记：记录服务框架设计模式
- **产出**：`cpp-service-core` v0.1 里程碑

---

## 第 5 周：服务工程化（4/6 – 4/12）

> 核心任务：把 `cpp-service-core` 从"能跑"升级到"像工程"

### 4/6（周一）

- [ ] 加错误处理框架：
  - 定义 `ErrorCode` 枚举
  - 定义 `Result<T>` 类型（类似 Rust 的 Result）
  - 统一异常处理：catch 住 worker 中的异常，返回错误响应而不是崩溃
- **产出**：错误处理框架

### 4/7（周二）

- [ ] 加超时控制：
  - 连接空闲超时（定时器 + timerfd）
  - 请求处理超时（worker 超时后返回 timeout 错误）
- [ ] 实现 `TimerQueue`：
  - 基于 `timerfd_create` + epoll
  - 支持 one-shot 和 repeating timer
- **产出**：超时机制完成

### 4/8（周三）

- [ ] 加基础指标：
  - `Metrics` 类：原子计数器
  - 指标项：总请求数、成功数、失败数、活跃连接数
  - 暴露指标接口（简单的 HTTP endpoint 或定时打印到日志）
- **产出**：指标收集完成

### 4/9（周四）

- [ ] 加 request id：
  - 每个请求生成唯一 ID
  - 日志全程携带 request id
  - 响应带回 request id
- [ ] 验证：通过日志可以追踪一个请求的完整生命周期
- **产出**：请求追踪链路

### 4/10（周五）

- [ ] 写 GoogleTest 单元测试：
  - `Codec` 编解码正确性
  - `Dispatcher` 分发正确性
  - `TimerQueue` 定时器触发
  - `ThreadPool` 任务执行
- [ ] 目标：核心模块测试覆盖
- **产出**：测试套件

### 4/11（周六）

- [ ] 写 benchmark 脚本：
  - 不同线程数（1/2/4/8）下的 QPS
  - 不同消息大小（64B/1KB/4KB）下的吞吐量
  - 长连接 vs 短连接对比
- [ ] 记录数据，画对比图
- **产出**：benchmark 报告

### 4/12（周日）

- [ ] 复盘本周
- [ ] 更新架构图：加入错误处理、超时、指标模块
- [ ] 更新 README v2
- [ ] 总结文档：《一个 C++ 服务端最小工程骨架是怎样的》
- **产出**：第 3 篇技术总结 + `cpp-service-core` v0.2 里程碑

---

## 第 6 周：HTTP 层 + LLM Gateway 预备（4/13 – 4/19）

> 核心任务：为第 5 月的 LLM Gateway 做技术准备

### 4/13（周一）

- [ ] 学习 `cpp-httplib`：
  - 写一个最小 HTTP server（GET/POST）
  - 写一个最小 HTTP client（请求外部 URL）
  - 理解 request/response 对象
- **产出**：HTTP 基础 demo

### 4/14（周二）

- [ ] 学习 `nlohmann/json` 深度用法：
  - struct 到 JSON 的自动序列化（`NLOHMANN_DEFINE_TYPE_INTRUSIVE`）
  - JSON patch、merge
  - 错误处理（parse 失败不崩溃）
- [ ] 模拟 OpenAI Chat Completion API 的请求/响应 JSON 结构
- **产出**：JSON 处理 demo

### 4/15（周三）

- [ ] 学习 Server-Sent Events (SSE) 协议：
  - 手动用 curl 请求一个 SSE 接口，观察数据格式
  - 理解 `data:`, `event:`, `id:` 字段
  - 理解 streaming response 和普通 response 的区别
- [ ] 用 cpp-httplib 实现一个 SSE server demo
- **产出**：SSE 理解 + demo

### 4/16（周四）

- [ ] 学习 libcurl 基础：
  - 同步请求
  - 设置 header、timeout
  - **streaming 回调**（`CURLOPT_WRITEFUNCTION`）
- [ ] 用 libcurl 写一个 streaming HTTP client
- **产出**：libcurl streaming demo

### 4/17（周五）

- [ ] 阅读 OpenAI API 文档（Chat Completions）：
  - 请求格式（model, messages, temperature, stream...）
  - 响应格式（非流式 vs 流式）
  - 错误码（rate limit, token limit, auth error...）
- [ ] 用 curl 或 libcurl 手动调用一次真实 LLM API（或兼容接口如 Ollama/vLLM）
- **产出**：API 交互理解

### 4/18（周六）

- [ ] 设计 LLM Gateway 的模块划分（文档 + 图）：
  ```
  llm-gateway/
  ├── api/         # HTTP 接口层
  ├── provider/    # 模型提供者抽象
  ├── router/      # 模型路由
  ├── middleware/   # 鉴权、限流、日志
  ├── config/      # 配置
  └── common/      # 公共工具
  ```
- [ ] 定义核心接口：
  - `Provider` 抽象基类
  - `Request` / `Response` 数据结构
  - `Router` 路由策略
- **产出**：LLM Gateway 设计文档

### 4/19（周日）

- [ ] 复盘本周
- [ ] 把 HTTP + JSON + SSE 知识整理成 Obsidian 笔记
- [ ] 规划下周 Gateway 编码任务
- **产出**：Gateway 预备完成

---

## 第 7 周：LLM Gateway v0.1（4/20 – 4/26）

> 核心任务：正式开始 `llm-gateway`，完成最小可用版本

### 4/20（周一）

- [ ] 建仓库 `llm-gateway`
- [ ] 初始化项目结构（按上周设计）
- [ ] 实现 `Provider` 抽象接口：
  ```cpp
  class Provider {
  public:
      virtual ~Provider() = default;
      virtual Result<ChatResponse> chatCompletion(const ChatRequest& req) = 0;
      virtual Result<void> chatCompletionStream(
          const ChatRequest& req,
          std::function<void(const StreamChunk&)> callback) = 0;
  };
  ```
- **产出**：项目骨架 + Provider 接口

### 4/21（周二）

- [ ] 实现 `MockProvider`：
  - 固定延迟后返回模拟响应
  - 流式模式下逐 chunk 回调
  - 可配置延迟、错误率（用于后续测试）
- [ ] 写测试验证 MockProvider
- **产出**：MockProvider 完成

### 4/22（周三）

- [ ] 实现 HTTP API 层：
  - `POST /v1/chat/completions`（兼容 OpenAI 格式）
  - 非流式：直接返回 JSON
  - 流式：返回 SSE
- [ ] 请求解析 + 参数校验
- **产出**：API 层完成，能用 curl 调通

### 4/23（周四）

- [ ] 实现 `Router`：
  - 根据 `model` 字段路由到对应 Provider
  - 配置文件定义路由规则
- [ ] 加 request id + 请求日志
- **产出**：路由 + 日志

### 4/24（周五）

- [ ] 加超时控制：
  - Provider 调用超时
  - 请求整体超时
- [ ] 加基础错误处理：
  - Provider 错误 → 标准错误响应
  - 参数错误 → 400
  - 内部错误 → 500
- **产出**：超时 + 错误处理

### 4/25（周六）

- [ ] 接入一个真实 Provider（选一个）：
  - **推荐 Ollama**：本地运行，免费，兼容 OpenAI API
  - 或 vLLM / LM Studio / 国内兼容接口
- [ ] 端到端测试：curl → gateway → provider → response
- [ ] 流式测试：观察 SSE 输出
- **产出**：真实 Provider 接入

### 4/26（周日）

- [ ] 复盘本周
- [ ] 写 README：项目介绍、架构图、使用方法、支持的 Provider
- [ ] 画请求流程图（Mermaid）
- [ ] 整理 Gateway 相关知识到 Obsidian
- **产出**：`llm-gateway` v0.1 里程碑

---

## 第 8 周：Gateway 加固 + 阶段收尾（4/27 – 4/30）

> 最后 4 天，加固 Gateway + 总复盘

### 4/27（周一）

- [ ] 加基础限流：
  - Token Bucket 算法
  - 按 API Key 限流
- [ ] 加基础鉴权：
  - 请求头 `Authorization: Bearer <key>`
  - 配置文件定义合法 key
- **产出**：限流 + 鉴权

### 4/28（周二）

- [ ] 加简单 retry：
  - Provider 超时或 5xx 时重试
  - 配置最大重试次数、重试间隔
  - 区分可重试错误 vs 不可重试错误
- **产出**：retry 机制

### 4/29（周三）

- [ ] 写 benchmark：
  - 并发请求下的 QPS（用 MockProvider）
  - 流式 vs 非流式延迟对比
- [ ] 更新 README + 架构图
- **产出**：benchmark 数据

### 4/30（周四）

- [ ] **阶段总复盘**：
  - 回顾 53 天的产出清单
  - 检查每个仓库的 README 是否完整
  - 检查每个仓库是否有测试
  - 列出 5 月需要继续做的事（Gateway v2 工程化）
- [ ] 输出《3-4 月学习复盘》
- [ ] 更新总体方案进度标记
- **产出**：阶段复盘文档

---

## 进度跟踪表

> 每完成一项打 ✅，未完成标注原因

| 周 | 日期 | 核心交付物 | 状态 |
|----|------|-----------|------|
| W1 | 3/9-3/15 | `cpp-core-lab` 仓库 + 6 个实验 | ⬜ |
| W2 | 3/16-3/22 | `cpp-threadpool` + benchmark + 技术总结 | ⬜ |
| W3 | 3/23-3/29 | reactor 框架 + 技术总结 | ⬜ |
| W4 | 3/30-4/5 | `cpp-service-core` v0.1 | ⬜ |
| W5 | 4/6-4/12 | `cpp-service-core` v0.2 + 技术总结 | ⬜ |
| W6 | 4/13-4/19 | HTTP/JSON/SSE 预备 + Gateway 设计 | ⬜ |
| W7 | 4/20-4/26 | `llm-gateway` v0.1 | ⬜ |
| W8 | 4/27-4/30 | Gateway 加固 + 阶段复盘 | ⬜ |

---

## 如果某周卡住了

1. **编码卡住**：先看参考实现（muduo 源码、开源 Gateway 项目），不要硬啃
2. **概念不理解**：写最小复现代码，不要只看文档
3. **时间不够**：砍掉"可选"任务，确保核心链路跑通
4. **整周落后**：下一周开头用 2 小时补上周最关键的 1 件事，然后继续当周计划，不要全部重做上周

---

## 5 月预告

完成本计划后，5 月的重点是：
- `llm-gateway` v2：加 fallback、circuit breaker、model routing 策略
- Docker 化：Dockerfile + docker-compose
- 开始准备简历第一个 bullet point
