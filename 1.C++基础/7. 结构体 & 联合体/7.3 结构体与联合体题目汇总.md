---
tags:
  - Cpp基础/结构体与联合体/题解
阅读次数: 1
---

# 7.3 结构体与联合体题目汇总

> 第七章 [[7.1 结构体|结构体]] 与 [[7.2 联合体|联合体]] 综合考察。覆盖定义与存储、成员访问、初始化、内存对齐与 padding、struct vs class、union 共享内存与类型双关、字节序、`std::variant`。
> 内存对齐的底层机制另见 [[6.7 指针结构体]]。
>
> **做法建议**：先把每一节做完，再展开 `参考答案` 折叠块对照。`sizeof`/对齐类题目务必能在纸上画出布局。
>
> 全卷 45 题：一~六为基础综合（选择 / 判断 / 读码 / 找错 / 简答 / 设计），七~八为深化（概念辨析进阶 / 大厂面试场景题）。

---

## 一、选择题（概念基础）

**1.** 常见平台下，`struct A { char c; int i; };` 的 `sizeof(A)` 是：
- A. 5　　B. 8　　C. 4　　D. 12

**2.** 关于 `struct` 和 `class`，正确的是：
- A. struct 不能有成员函数　　B. struct 默认 `private`，class 默认 `public`
- C. struct 默认 `public`，class 默认 `private`　　D. 二者完全没有区别

**3.** `union U { int i; double d; char c; };` 的 `sizeof(U)` 是：
- A. 4　　B. 8　　C. 13　　D. 16

**4.** `p->member` 等价于：
- A. `*p.member`　　B. `(*p).member`　　C. `*(p.member)`　　D. `&p.member`

**5.** 关于联合体，正确的是：
- A. 所有成员可同时有效　　B. 同一时刻只有一个成员有效（最后写入的）
- C. 成员各自占独立内存　　D. union 不能放数组

**6.** 关于 C++20 指定初始化器 `S s = {.a=1, .b=2};`，正确的是：
- A. 可以任意乱序，和 C 一样　　B. 必须按成员声明顺序书写
- C. 不能跳过任何成员　　D. C++ 不支持指定初始化器

**7.** `union U { int i; char buf[5]; };` 的 `sizeof(U)` 和 `alignof(U)`：
- A. 5, 1　　B. 5, 4　　C. 8, 4　　D. 8, 1

**8.** 小端系统上 `union { int i; char c; } u; u.i = 0x12345678;` 后 `u.c` 是：
- A. 0x12　　B. 0x78　　C. 0x34　　D. 未定义

**9.** C++17 中类型安全、替代 `union` 的设施是：
- A. `std::any`　　B. `std::optional`　　C. `std::variant`　　D. `std::tuple`

**10.** 下列哪个**不能**出现在匿名联合体里：
- A. `int` 成员　　B. 数组成员　　C. 成员函数　　D. `float` 成员

> [!success]- 参考答案（第一节）
> 1. **B**。`char`(1) + 3 padding + `int`(4) = 8（`int` 须 4 字节对齐）。
> 2. **C**。唯一实质区别是默认成员访问权限和默认继承权限：struct 为 `public`，class 为 `private`。struct 同样可以有成员函数、构造、继承、多态。
> 3. **B**。最大成员 `double` 为 8 字节、对齐 8，`sizeof` = 8。
> 4. **B**。`->` 是"先解引用再取成员"。注意 `*p.member` 因 `.` 优先级更高会被解析成 `*(p.member)`，对指针 `p` 是错的。
> 5. **B**。union 所有成员共享同一内存，活动成员是最后写入的那个。
> 6. **B**。C++20 的指定初始化器要求顺序与声明一致（C 允许乱序，C++ 不允许）。
> 7. **C**。`buf[5]` 虽达 5 字节，但 `int` 要求 4 字节对齐 → 尾部补齐到 8；`alignof` 取成员最严格者 = 4。
> 8. **B**。小端低地址放低字节，内存为 `[78][56][34][12]`，`c` 读最低地址字节 = 0x78。
> 9. **C**。`std::variant` 是类型安全的"带标签联合"。
> 10. **C**。匿名联合体只能含非静态数据成员，不能有成员函数，也不能有 `private`/`protected` 成员。

---

## 二、概念辨析（判断对错并说明理由）

**11.** 结构体对象一定分配在栈上。

**12.** `union` 的大小一定等于其最大成员的大小。

**13.** `struct` 不能定义成员函数，要用成员函数必须改成 `class`。

**14.** 写入 `union` 的一个成员后，读取另一个成员，在标准 C++ 中是良定义行为。

**15.** 把结构体用 `send(fd, &header, sizeof(header), 0)` 直接发出去，是可靠的跨平台协议传输方式。

**16.** `union` 也可能包含 padding。

**17.** `struct B { int i; char c1; char c2; };` 比 `struct A { char c1; int i; char c2; };` 占用内存更小。

**18.** `std::variant<int, double>` 通常会在堆上分配它存储的值。

> [!success]- 参考答案（第二节）
> 11. **错**。结构体只是类型，对象位置由创建方式决定：局部非静态→栈，`new`→堆，全局/`static`→静态存储区，成员对象跟随外层对象。
> 12. **错**。是"**至少**能容纳最大成员"，还要按最严格成员的对齐补齐尾部 padding。如 `union{int i; char buf[5];}` 大小是 8 而非 5。
> 13. **错**。`struct` 完全可以有成员函数、构造/析构、继承、多态。它和 `class` 只差默认访问/继承权限。
> 14. **错**。标准 C++ 中读取非活动成员是未定义行为（这一点 C 与 C++ 不同，C99 起允许）。多数编译器作为扩展支持，但不可移植。正确做法见第 39 题。
> 15. **错**。结构体内存布局含 padding（可能是未初始化垃圾）、跨平台/编译器/ABI 布局可能不同、多字节整数有字节序问题。应使用定长类型 + 手动序列化 + 约定字节序。
> 16. **对**。union 通常是**尾部 padding**，用于保证 union 数组中每个元素都满足整体对齐。
> 17. **对**。`B`（int,char,char）= 8；`A`（char,int,char）= 12。把小成员集中、大成员靠前可减少中间 padding。
> 18. **错**。`std::variant` 把当前值**内联存储**在自身（对齐存储），不额外堆分配（除非某个候选类型自己会分配）。

---

## 三、代码输出预测

**19.**
```cpp
struct A { char c1; int i; char c2; };
struct B { int i; char c1; char c2; };
std::cout << sizeof(A) << " " << sizeof(B) << "\n";   // 常见 4 字节对齐
```

**20.**
```cpp
union FloatInt { float f; int i; };
FloatInt fi;
fi.i = 42;
fi.f = 3.14f;          // 覆盖 i
std::cout << fi.f << "\n";
// 此时读 fi.i 还是 42 吗？
```

**21.**
```cpp
struct Point { int x; int y; };
Point p = {.x = 1, .y = 2};
Point* pp = &p;
std::cout << pp->x << " " << (*pp).y << "\n";
```

**22.**
```cpp
struct Rectangle {
    int w, h;
    int area() { return w * h; }
};
Rectangle r = {3, 4};
std::cout << r.area() << "\n";
```

**23.**
```cpp
union U { int i; char buf[5]; };
std::cout << sizeof(U) << " " << alignof(U) << "\n";
```

**24.**
```cpp
struct S { int v; };
void byVal(S s)  { s.v = 100; }
void byRef(S& s) { s.v = 100; }
int main() {
    S a{1}, b{1};
    byVal(a);
    byRef(b);
    std::cout << a.v << " " << b.v << "\n";
}
```

**25.**
```cpp
#include <variant>
std::variant<int, std::string> v = 10;
std::cout << std::holds_alternative<int>(v) << " ";
v = std::string("hi");
std::cout << std::holds_alternative<int>(v) << "\n";
```

**26.**
```cpp
struct Tagged {
    enum { INT, FLT } type;
    union { int i; float f; };   // 匿名联合体
};
Tagged t;
t.type = Tagged::FLT;
t.f = 2.5f;
std::cout << (t.type == Tagged::FLT ? t.f : t.i) << "\n";
```

> [!success]- 参考答案（第三节）
> 19. `12 8`。A：char+3pad+int+char+3tail = 12；B：int+char+char+2tail = 8。
> 20. 输出 `3.14`。写 `fi.f` 后活动成员是 `f`；此时 `fi.i` **不再是 42**，读它是 UB（其位模式变成 3.14f 的二进制表示）。
> 21. 输出 `1 2`。`pp->x` 与 `(*pp).y` 都正确访问成员。
> 22. 输出 `12`。struct 可以有成员函数，`area()` 返回 `3*4`。
> 23. 输出 `8 4`。尾部补齐到 8，对齐取成员最严格者 `int`=4。
> 24. 输出 `1 100`。`byVal` 改的是副本，`a` 不变；`byRef` 改的是本体，`b` 变 100。
> 25. 输出 `1 0`。初值是 `int` → `holds_alternative<int>` 为真(1)；赋 string 后变为假(0)。
> 26. 输出 `2.5`。`type` 作判别标签，按 `FLT` 读 `f`，匿名联合体成员可直接用名字访问。

---

## 四、找错与修复（指出 bug 并改正）

**27.**
```cpp
struct S { int id; };
S* p = new S;
std::cout << *p.id << "\n";   // 想打印 id
```

**28.**
```cpp
union U { int i; double d; };
U u;
u.i = 5;
std::cout << u.d << "\n";     // 想"顺便"看看 d
```

**29.**
```cpp
struct Student { int id; char name[20]; float gpa; };
Student s = {.name = "Tom", .id = 1, .gpa = 3.9};  // C++20
```

**30.**
```cpp
struct PacketHeader { char type; int length; short checksum; };
PacketHeader h{ 'A', 100, 7 };
send(fd, &h, sizeof(h), 0);   // 发给另一台机器的服务
```

**31.**
```cpp
union MyUnion { int i; float f; };
MyUnion u = {1, 3.14f};       // 想同时给 i 和 f 初始化
```

> [!success]- 参考答案（第四节）
> 27. **优先级错误**：`*p.id` 被解析为 `*(p.id)`，但 `p` 是指针没有 `.id`，编译错误。改为 `p->id` 或 `(*p).id`。（用完记得 `delete p;`）
> 28. **读非活动成员**：活动成员是 `i`，读 `u.d` 是未定义行为。若确实要看 `i` 的位模式，应先想清楚意图；要类型双关请用 `memcpy`/`std::bit_cast`。
> 29. **指定初始化器乱序**：C++20 要求按声明顺序 `id, name, gpa`。改为 `{.id=1, .name="Tom", .gpa=3.9f}`。
> 30. **直接发结构体不可靠**：padding（含未初始化字节）、字节序、跨平台布局差异。改用定长类型 `uint8_t/uint32_t/uint16_t` 并按协议逐字段序列化、统一字节序（如网络序）。
> 31. **union 只能初始化一个成员**：它们共享内存，`{1, 3.14f}` 非法。写 `MyUnion u = {1};`（初始化 `i`）或 C++20 `MyUnion u = {.f = 3.14f};`。

---

## 五、简答题

**32.** `struct` 和 `class` 在 C++ 中有什么区别？

**33.** 联合体为什么能节省内存？它的代价（限制）是什么？

**34.** 简述结构体内存对齐的三条基本规则。

**35.** 为什么调换结构体成员的声明顺序会改变 `sizeof`？给出减少 padding 的经验法则。

**36.** `.` 和 `->` 有什么区别？为什么 `*p.member` 通常是错的？

**37.** 传结构体参数时，按值 / `const T&` / `T&` / `T*` 分别在什么场景下用？

> [!success]- 参考答案（第五节）
> 32. 唯一实质区别是**默认访问权限和默认继承权限**：`struct` 默认 `public`，`class` 默认 `private`。其余功能（成员函数、构造析构、继承、多态、模板）完全相同。惯例：`struct` 偏数据聚合，`class` 偏封装与不变量。
> 33. 因为所有成员**共享同一块内存**，大小只需容纳最大成员，而不是各成员之和。代价：同一时刻只有一个成员有效；读非活动成员是 UB；union 自己不知道当前存的是哪个类型（需要外部"标签"配合）；含非平凡类型成员时需手动管理构造析构。
> 34. ① 每个成员的起始偏移须是其自身对齐要求的整数倍（必要时前面插 padding）；② 结构体整体大小须是其最大成员对齐要求的整数倍（必要时尾部插 padding）；③ 结构体的对齐要求等于其成员中最严格的对齐要求。
> 35. padding 是为了让每个成员落在满足其对齐的偏移上。小成员散落在大成员之间会逼出更多中间 padding；把大对齐成员靠前、小成员集中，可减少填充。经验法则：成员按对齐从大到小排列（`double`/指针 → `int` → `short` → `char`）。
> 36. `.` 用于对象访问成员，`->` 用于指针访问成员（`p->m` 等价 `(*p).m`）。`.` 优先级高于 `*`，所以 `*p.member` 被解析为 `*(p.member)`；对指针 `p` 没有 `.member`，应写 `p->member` 或 `(*p).member`。
> 37. 只读且对象较大 → `const T&`（零拷贝、防改）；必须修改且对象一定存在 → `T&`；对象可能为空 → `T*`（判 `nullptr`）；对象很小或需要独立副本 → 按值。要转移所有权 → `T&&` + `std::move`。

---

## 六、综合设计与手写代码

**38.** 用结构体 + `enum` 标签 + 联合体，手写一个**带标签联合（tagged union）**，能安全地存 `int` 或 `double`，并写一个按标签读取的函数。再说明 `std::variant` 相比它好在哪。

**39.** 写一个函数 `isLittleEndian()`，用 `union` 判断当前系统是否小端。再说明为什么用 `union` 做"类型双关"在标准 C++ 中其实是 UB，正确的可移植写法是什么。

**40.** 给定下面的结构体，在不改变成员类型的前提下重排成员，使 `sizeof` 最小，并写出两种布局的字节数。
```cpp
struct Bad { char a; double b; char c; int d; short e; };
```

**41.** 设计一个跨平台的网络包头 `PacketHeader`（含 1 字节类型、4 字节长度、2 字节校验和），给出**安全序列化**到字节缓冲区的思路（不直接 `memcpy` 整个结构体）。

> [!success]- 参考答案（第六节）
> **38.**
> ```cpp
> struct Number {
>     enum Tag { INT, DOUBLE } tag;
>     union { int i; double d; };
> };
> double readAsDouble(const Number& n) {
>     return n.tag == Number::INT ? double(n.i) : n.d;   // 按标签读，避免读错成员
> }
> ```
> `std::variant` 的优势：自动维护"当前类型"、访问错类型抛 `std::bad_variant_access`、支持 `std::visit` 模式匹配、对非平凡类型自动管理构造析构——比手写 tagged union 更安全省心。
>
> **39.**
> ```cpp
> bool isLittleEndian() {
>     union { uint32_t i; uint8_t c[4]; } u;
>     u.i = 0x01020304;
>     return u.c[0] == 0x04;     // 小端：最低地址放最低字节
> }
> ```
> 为什么是 UB：标准 C++ 规定读非活动成员是未定义行为（GCC/Clang 作为扩展支持，但不保证可移植）。可移植做法：用 `std::memcpy` 取字节，或 C++20 的 `std::bit_cast`：
> ```cpp
> uint32_t x = 0x01020304;
> uint8_t b0;
> std::memcpy(&b0, &x, 1);
> bool little = (b0 == 0x04);
> // 或 C++20：if constexpr (std::endian::native == std::endian::little) ...
> ```
>
> **40.** `Bad`（char,double,char,int,short）含大量中间 padding，常见大小 **24** 字节。重排为对齐从大到小：
> ```cpp
> struct Good { double b; int d; short e; char a; char c; };  // 8+4+2+1+1=16
> ```
> 常见大小 **16** 字节（尾部补齐到 8 的倍数）。规则：大对齐成员靠前、小成员集中。
>
> **41.** 思路：①用定长类型定义逻辑字段 `uint8_t type; uint32_t length; uint16_t checksum;`；②准备 `uint8_t buf[7]`；③逐字段写入，多字节字段统一转**网络字节序**（`htonl`/`htons`）：
> ```cpp
> uint8_t buf[7];
> buf[0] = type;
> uint32_t len_n = htonl(length);  std::memcpy(buf + 1, &len_n, 4);
> uint16_t crc_n = htons(checksum); std::memcpy(buf + 5, &crc_n, 2);
> send(fd, buf, sizeof(buf), 0);   // 7 字节，无 padding、字节序确定
> ```
> 接收端按相同偏移和字节序反序列化。这样布局与平台/编译器无关。

---

## 七、概念辨析进阶

**42.** "类型双关 (type punning)" 通过 union 实现，在 C 和 C++ 标准里的合法性有什么不同？C++ 里推荐的可移植替代是什么？

**43.** `sizeof` 和 `alignof` 各表示什么？对 `union{int i; char buf[5];}` 它们分别是多少？

**44.** 什么是**位域 (bit-field)**？写一个用位域压缩存储的例子，并指出它的可移植性陷阱。

**45.** 解释 trivially-copyable / standard-layout 两个概念，为什么它们和"能不能 `memcpy` 整个对象""能不能跨语言/跨进程传"有关？

> [!success]- 参考答案（第七节）
> 42. 在 **C（C99 起）**中，通过 union 读非活动成员做类型双关是**允许**的、有明确语义。在 **标准 C++** 中这是**未定义行为**（只有最后写入的成员可读）。GCC/Clang 作为扩展支持 union 类型双关，但要可移植应使用 `std::memcpy` 或 C++20 `std::bit_cast<T>(x)`（要求两类型同大小、trivially-copyable）。
> 43. `sizeof` 是对象占用的字节数；`alignof` 是该类型要求的对齐字节数（地址须为它的整数倍）。对 `union{int i; char buf[5];}`：`sizeof = 8`（5 向上补齐到 4 的倍数），`alignof = 4`（取成员最严格的 `int`）。
> 44.
> ```cpp
> struct Flags {
>     unsigned ready : 1;   // 占 1 bit
>     unsigned mode  : 3;   // 占 3 bit
>     unsigned count : 12;  // 占 12 bit
> };
> ```
> 用于在一个整型里压缩多个小字段（标志位、协议字段）。陷阱：**位的排布顺序、是否跨字节、填充都由实现定义**，不同编译器/平台/字节序结果可能不同，所以位域**不适合**直接用于跨平台二进制协议；跨平台应自己用移位 + 掩码手动打包。
> 45.
> - **trivially-copyable**：对象可以用 `memcpy` 逐字节复制而语义不变（无自定义拷贝/移动/析构、无虚函数等）。
> - **standard-layout**：内存布局规则简单、与 C 兼容（成员访问权限一致、无虚函数、布局可预测）。
> 只有 trivially-copyable 才能安全 `memcpy` 整个对象；只有 standard-layout 才适合跨语言（与 C 互操作）、跨进程（共享内存）传递。含 `std::string`/虚函数的类两者都不满足，直接 `memcpy` 会破坏内部指针。

---

## 八、大厂面试场景题

> 偏真实工程，含网络/系统/性能方向。

**46.** 同事把一个 C++ 结构体直接 `memcpy` 进缓冲区通过 socket 发给另一台不同架构的机器，对端解析出来字段全乱。请系统地列出**所有可能的原因**和**正确做法**。

**47.** 项目里有个"值可能是 int、string 或一组坐标"的配置项，老代码用 `union` + 手写 `enum` 标签实现，经常因为忘了更新标签而读错类型导致崩溃。你会怎么用现代 C++ 重构？怎么遍历处理？

**48.** 一个 `struct` 在 64 位机上 `sizeof` 是 40 字节，里面只有几个 `int` 和 `bool`。怎么会这么大？你怎么排查并优化？这对一个要装进数组、被 GPU/SIMD 批量处理的热数据结构有什么意义？

**49.** 嵌入式/驱动里常见这种写法：把一个寄存器地址 `reinterpret_cast` 成结构体指针，或用 union 把一个 32 位寄存器拆成若干位域来读写。这样做的好处和风险分别是什么？

**50.** 为什么说"`union` 知道大小但不知道类型"是它最大的安全隐患？`std::variant` 是如何解决的？它在异常安全上有什么代价（valueless_by_exception）？

> [!success]- 参考答案（第八节·上）
> 46. 原因：① **padding** 字节布局且可能含未初始化垃圾；② **字节序**（大端/小端）不同，多字节整数被反转；③ **类型宽度**不固定（`int`/`long` 在不同 ABI 大小不同）；④ **对齐/ABI** 差异导致成员偏移不同；⑤ 含指针/`std::string` 等非平凡成员时，传过去的是本机地址，毫无意义。正确做法：定义**定长类型**字段（`uint8_t/uint16_t/uint32_t`）、**手动逐字段序列化**、统一**网络字节序**（`htonl/htons`）、避免传指针/复杂对象，或直接用 Protobuf/FlatBuffers 等成熟序列化库。
> 47. 用 `std::variant<int, std::string, Point>` 替换手写 tagged union——它自动记录当前类型，访问错类型抛异常而非静默崩溃。遍历处理用 `std::visit` + 重载的 visitor：
> ```cpp
> std::variant<int, std::string, Point> cfg = ...;
> std::visit([](auto&& v){ /* 按实际类型处理 */ }, cfg);
> // 或 std::holds_alternative<T> / std::get_if<T> 做条件分支
> ```
> 好处：编译期类型集合固定、运行期类型安全、无需手动同步标签。
> 48. 多半是**内存对齐 padding** 把结构撑大了（如 `bool` 后跟 `double`/指针、或成员顺序差导致大量中间填充）。排查：逐成员算偏移，或用 `offsetof`/编译器 `-Wpadded` 警告查看填充。优化：按对齐从大到小重排成员、把 `bool`/小枚举集中、必要时用更小的定长类型。对热数据结构意义重大：更小的结构体 → 一条缓存行装更多元素 → 更高缓存命中率；对 GPU/SIMD，紧凑且常配合 **SoA 布局** 能显著提升合并访存与向量化效率。

> [!success]- 参考答案（第八节·下）
> 49. 好处：直接、零拷贝地按"结构化视图"读写硬件寄存器/协议字段，代码可读性比一堆移位掩码好。风险：① 严格依赖**具体编译器的位域/对齐/字节序实现**，换平台可能错位；② 标准 C++ 上属未定义/实现定义行为（`reinterpret_cast` 访问不兼容类型违反严格别名规则）；③ 需要 `volatile` 防止编译器优化掉对寄存器的读写。所以这类代码通常限定在已知工具链+已知硬件，且要配合 `volatile` 和编译器扩展。
> 50. `union` 只保证"内存够大放下最大成员"，但**不记录当前存的是哪个类型**，全靠程序员自觉/外部标签，一旦读错成员就是 UB、静默出错。`std::variant` 内部维护一个**类型索引（标签）**，访问时校验类型、错了就抛 `std::bad_variant_access`，并支持 `std::visit` 安全分发。代价：当给 variant 赋值的过程中抛异常、且无法回退到旧值时，variant 可能进入 `valueless_by_exception()` 的特殊空状态（此时 `index()` 返回 `variant_npos`），需要在异常敏感代码里留意。

---

## 九、自测对照表

做完后用此表定位薄弱环节：

| 题号 | 考点 | 对应笔记 |
|------|------|---------|
| 1, 19, 34, 35, 40 | 内存对齐 / padding / 成员顺序 | [[7.1 结构体]]、[[6.7 指针结构体]] |
| 2, 13, 32 | struct vs class | [[7.1 结构体]] |
| 4, 27, 36 | `.` / `->` / 优先级陷阱 | [[7.1 结构体]] |
| 6, 29 | 指定初始化器（C++20） | [[7.1 结构体]]、[[7.2 联合体]] |
| 11 | 结构体存储位置 | [[7.1 结构体]]、[[6.6 堆与栈的区别]] |
| 22 | 结构体成员函数 | [[7.1 结构体]] |
| 24, 37 | 参数传递（值/引用/指针） | [[7.1 结构体]] |
| 3, 5, 7, 12, 16, 23, 43 | union 大小 / 对齐 / 活动成员 | [[7.2 联合体]] |
| 8, 20, 28, 39, 42 | 类型双关 / 字节序 / UB | [[7.2 联合体]]、[[3.3 位运算]] |
| 10, 26, 31 | 匿名联合 / 初始化 / tagged union | [[7.2 联合体]] |
| 9, 25, 47, 50 | std::variant | [[7.2 联合体]] |
| 15, 30, 41, 46 | 网络协议序列化 / 跨平台 | [[7.1 结构体]] |
| 44, 45 | 位域 / trivially-copyable / standard-layout | [[7.1 结构体]]、[[7.2 联合体]] |
| 48, 49 | 性能 / 嵌入式 / AI Infra | [[7.1 结构体]]、[[6.6 堆与栈的区别]] |

---

## 相关链接

- [[7.1 结构体]] — 结构体定义、存储、对齐、参数传递
- [[7.2 联合体]] — 共享内存、类型双关、std::variant
- [[6.7 指针结构体]] — 指针访问结构体与内存对齐汇编分析
- [[6.10 指针题目汇总]] — 上一章指针综合题集
