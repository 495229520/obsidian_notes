---
tags:
  - 项目/远控系统
git: ""
---

# Debug-XXX：{{title}}

> **现象**：一句话描述用户可见的症状（崩溃 / 卡死 / 数据错误 / 功能失效）。
> **根因**：一句话说清楚本质原因（不是表面现象，而是代码层面为什么出错）。

<!--
使用说明：
1. 编号规则：在 Debug 经验汇总与方法论.md 的索引表中查看下一个可用编号
2. 标题格式：Debug-XXX 问题的一句话概括（如"位运算符误写导致鼠标操作失效"）
3. 简单 Bug（单一根因、修复代码 < 10 行）：保留"现象 → 调试过程 → 根因 → 修复 → 调试经验"，删除其余章节
4. 复杂 Bug（多根因、涉及线程/生命周期/架构）：保留全部章节，展开写
5. 写完后同步更新 Debug 经验汇总与方法论.md 的索引表和分类统计
-->

---

## Bug 基本信息

| 项目 | 内容 |
|------|------|
| **编号** | Debug-XXX |
| **严重程度** | 🔴 崩溃 / 🔴 卡死 / 🔴 功能失效 / 🟡 数据丢失 / 🟡 资源泄漏 |
| **分类** | 多线程竞争 / TCP 粘包 / 语法陷阱 / 资源管理 / 边界条件 / MFC 控件 / ... |
| **commit** | `commit_hash` |
| **出现场景** | 描述触发条件 |

---

## 现象描述

<!-- 用户/开发者可直接观察到的症状，越具体越好 -->

1. 操作步骤 1
2. 操作步骤 2
3. 观察到的异常现象

---

## 调试过程

<!-- 按实际排查顺序记录，展示"如何一步步缩小范围" -->

### 第一步：初步定位

<!-- 排查方向：客户端 UI → 客户端网络 → TCP 传输 → 服务端网络 → 服务端逻辑 -->

排查了什么、看到了什么、排除了什么。

### 第二步：缩小范围

加了什么日志 / 断点，观察到了什么关键现象。

```cpp
// 关键的调试代码或 TRACE 输出
TRACE("var=%d\r\n", var);  // 输出结果：var=0（预期应为 1）
```

### 第三步：定位根因

从现象推导出根本原因的逻辑链。

---

## 根因分析

<!-- 简单 Bug 可以压缩成一段代码 + 一句解释；复杂 Bug 展开写推理过程 -->

**问题代码**：

```cpp
// ❌ 问题描述
问题代码;  // ← 标注出错的具体位置和原因
```

**为什么这段代码会出问题**：

<!-- 复杂 Bug 在这里展开分析：竞争时序、状态机、内存布局、协议边界等 -->

```
推理链 / 竞争时序 / 内存布局：
1. 第一步发生了什么
2. 第二步导致了什么
3. 最终表现为什么症状
```

---

## 修复方案

```cpp
// ✅ 修复后的代码 + 注释说明修复逻辑
修正代码;
```

**关键点**：
- 为什么修复后是正确的
- 修复覆盖了哪些边界情况

---

## 修复效果

<!-- 简单 Bug 可删除本节；复杂 Bug 用文字或图示对比修复前后的行为差异 -->

```
修复前：
  触发条件 → 错误行为 → 症状

修复后：
  触发条件 → 正确行为 → 正常结果
```

---

## 底层原理

<!-- 可选章节：当 Bug 涉及值得深挖的底层机制时保留（如空指针+偏移量崩溃、TCP 流式语义、静态初始化时序） -->

### 原理名称

解释这个 Bug 背后的底层机制，帮助举一反三。

---

## 调试经验

| 经验 | 说明 |
|------|------|
| **教训 1** | 一句话总结可复用的调试经验 |
| **教训 2** | 一句话总结可复用的调试经验 |

---

## 关联笔记

- [[版本笔记名称]] — 本次修复所在的版本记录
- [[相关 Debug 日志]] — 同类或同批次修复的 Bug

---

## 更新记录

| 日期 | 变更 |
|------|------|
| {{date}} | 初始版本：基于提交 `commit_hash` 记录问题根因与修复 |
