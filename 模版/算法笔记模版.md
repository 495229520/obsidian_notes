---
tags:
  - 算法/笔记
  - 算法/力扣
题号: "{{题号}}"
难度: "{{难度}}"
阅读次数: 0
---

# {{题号}}. {{题目名称}}

> 用 1-2 句话说清楚：这道题要解决什么问题、输入输出是什么、最终要返回什么结果。

<!--
使用说明
1. 本模板用于算法题 / 力扣题解笔记。
2. 必须先讲“可用算法与数据结构”，再进入详细步骤和源码。
3. STL / 数据结构链接只保留本题真正用到的，避免堆砌无关链接。
4. 如果题目简单，不需要多张图，可以只保留一张 SVG；如果完全不需要图，再删除 SVG 章节。
5. 代码注释重点写“为什么这样做”和“这一步在算法中的作用”，不要写空泛注释。
6. 不适用的章节直接删掉，不要留下空标题。
-->

---

## 1. 题目概览

### 1.1 题目描述

用自己的话重述题目，重点说明：

1. 输入是什么
2. 输出是什么
3. 题目真正要求的目标是什么

### 1.2 输入输出与约束

| 项目 | 内容 |
|------|------|
| 输入 | `{{输入说明}}` |
| 输出 | `{{输出说明}}` |
| 数据范围 | `{{数据范围}}` |
| 特殊要求 | `{{是否要求原地、是否要求 O(1) 空间、是否有重复值等}}` |

> [!warning]
> 这里写最容易误解的题意，例如“第 k 大不是第 k 个不同元素”“子数组必须连续”“不能改变原数组”等。

### 1.3 示例

#### 1.3.1 示例一

```text
输入：{{示例输入}}
输出：{{示例输出}}
解释：{{示例解释}}
```

#### 1.3.2 示例二

```text
输入：{{示例输入}}
输出：{{示例输出}}
解释：{{示例解释}}
```

---

## 2. 可用算法与数据结构

### 2.1 可选解法总览

先列出这道题可能使用的算法和数据结构，再说明为什么最终选择其中一种。

| 方案 | 核心算法 | 核心数据结构 | 是否适合本题 | 原因 |
|------|----------|--------------|--------------|------|
| 方案一 | {{算法一}} | {{数据结构一}} | 是 / 否 | {{适合或不适合的原因}} |
| 方案二 | {{算法二}} | {{数据结构二}} | 是 / 否 | {{适合或不适合的原因}} |
| 方案三 | {{算法三}} | {{数据结构三}} | 是 / 否 | {{适合或不适合的原因}} |

### 2.2 本题采用的算法

本题最终采用：**{{本题核心算法}}**。

选择它的原因：

1. {{原因一：复杂度更优 / 更符合题目约束}}
2. {{原因二：实现更直接 / 状态更清晰}}
3. {{原因三：更容易处理边界情况}}

### 2.3 本题使用的数据结构

| 数据结构 | 在本题中的作用 | 为什么需要它 |
|----------|----------------|--------------|
| `{{数据结构一}}` | {{作用}} | {{原因}} |
| `{{数据结构二}}` | {{作用}} | {{原因}} |

### 2.4 相关 STL / 算法笔记引用

> [!note]
> 下面是可选引用清单。写具体题解时，只保留源码中真正使用到的 STL、算法或数据结构。

- [[14.1 vector]] — 动态数组、下标访问、连续存储
- [[14.2 queue]] — 队列、优先队列相关用法
- [[14.3 list及其与其他结构的区别]] — 链表结构与容器差异
- [[14.4 set]] — 有序集合、去重、自动排序
- [[14.5 map]] — 键值映射、计数、索引关系
- [[14.6 string]] — 字符串处理
- [[14.7 迭代器]] — STL 区间与遍历方式
- [[14.8 容器适配器]] — `stack` / `queue` / `priority_queue`
- [[14.9 函数对象]] — 比较器、自定义排序规则
- [[14.10 泛型算法与绑定器]] — 泛型算法与函数绑定
- [[14.11 算法]] — `sort`、`lower_bound`、`nth_element` 等算法
- [[14.12 高精度]] — 大整数或高精度计算
- [[14.13 前缀和]] — 前缀和与区间查询
- [[14.15 动态规划]] — 状态定义、状态转移、DP 表
- [[14.16 常见排序算法与STL排序]] — 排序算法与 STL 排序

---

## 3. 解题步骤

### 3.1 核心思路

先用 2-4 段短文字把整体思路讲顺：

1. 如何把题目转换成更容易处理的问题
2. 算法过程中维护什么状态
3. 每一步如何让答案更接近最终结果
4. 为什么这个过程不会漏掉正确答案

### 3.2 详细步骤

#### 3.2.1 第一步：初始化状态

说明初始变量、容器、边界、指针或 DP 状态如何设置。

```text
{{初始化示意}}
```

#### 3.2.2 第二步：遍历 / 递归 / 状态转移

说明主流程如何推进：

1. 当前处理哪个元素或状态
2. 如何更新数据结构
3. 如何更新候选答案
4. 什么情况下进入下一步或回退

#### 3.2.3 第三步：得到答案

说明循环结束、递归返回或状态表填完后，答案从哪里取。

### 3.3 示例推演

用一个具体输入手动走一遍关键步骤。

| 步骤 | 当前元素 / 状态 | 数据结构变化 | 当前答案 | 说明 |
|------|-----------------|--------------|----------|------|
| 1 | {{状态}} | {{变化}} | {{答案}} | {{说明}} |
| 2 | {{状态}} | {{变化}} | {{答案}} | {{说明}} |
| 3 | {{状态}} | {{变化}} | {{答案}} | {{说明}} |

---

## 4. SVG 图解

### 4.1 算法流程图

> [!tip]
> SVG 文件放在 `图片/SVG/`。力扣题建议命名为 `5_1_{{题号}}_1.svg`、`5_1_{{题号}}_2.svg`。

![[5_1_{{题号}}_1.svg|760]]

### 4.2 关键状态变化

这里说明 SVG 中每个阶段对应的算法状态，例如：

1. 指针如何移动
2. 窗口如何扩张或收缩
3. 堆、栈、队列、哈希表如何变化
4. DP 表如何从已知状态推出未知状态

> [!note]
> SVG 的第一个子元素应包含背景矩形：
>
> ```svg
> <rect width="100%" height="100%" fill="rgb(245, 244, 237)"/>
> ```

---

## 5. 源码实现

### 5.1 完整 C++ 代码

```cpp
class Solution {
public:
    {{返回类型}} {{函数名}}({{参数列表}}) {
        // 1. 初始化核心变量 / 数据结构，让算法有明确的起点
        // 2. 按照算法流程处理每个元素、区间或状态
        // 3. 返回题目要求的最终答案
    }
};
```

### 5.2 代码中的关键注释

| 注释位置 | 解释重点 |
|----------|----------|
| 初始化部分 | 为什么这些变量或容器需要这样初始化 |
| 主循环 / 递归部分 | 每次迭代维护了什么不变量 |
| 更新答案部分 | 为什么此时可以更新答案 |
| 返回结果部分 | 返回值为什么就是题目要求的结果 |

---

## 6. 源码详解

### 6.1 核心变量说明

| 变量 / 数据结构 | 含义 | 生命周期 | 为什么需要它 |
|-----------------|------|----------|--------------|
| `{{变量一}}` | {{含义}} | {{生命周期}} | {{原因}} |
| `{{变量二}}` | {{含义}} | {{生命周期}} | {{原因}} |

### 6.2 主流程拆解

#### 6.2.1 初始化

说明初始化代码对应算法步骤中的哪一部分，以及它建立了什么初始状态。

#### 6.2.2 主循环 / 递归过程

按代码执行顺序解释：

1. 每轮循环处理什么
2. 判断条件为什么这样写
3. 数据结构如何变化
4. 答案何时更新

#### 6.2.3 返回结果

说明最终返回值来自哪个变量或容器，以及为什么它已经满足题目要求。

### 6.3 易错点

1. {{易错点一}}
2. {{易错点二}}
3. {{易错点三}}

> [!warning]
> 如果边界条件容易出错，必须写清楚：空数组、单元素、重复值、负数、越界、溢出、递归终止条件等。

---

## 7. 复杂度分析

### 7.1 时间复杂度

- 时间复杂度：`{{时间复杂度}}`
- 原因：{{说明每个元素处理几次、排序 / 堆 / 递归 / DP 的主要开销}}

### 7.2 空间复杂度

- 空间复杂度：`{{空间复杂度}}`
- 原因：{{说明额外容器、递归栈、DP 表或原地操作的空间开销}}

---

## 8. 相关链接

- {{同类题目}} — {{为什么相关}}
- {{基础算法笔记}} — {{补充说明}}
- {{STL笔记}} — {{源码中对应的 STL 用法}}
