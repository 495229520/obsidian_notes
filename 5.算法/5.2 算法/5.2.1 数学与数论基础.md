---
tags:
  - 算法/数学
  - 算法/数论
  - 算法
阅读次数: 0
---

# 5.2.1 数学与数论基础

> 数学题不一定每场都考，但一旦出现，通常希望你能快速写出稳定模板：最大公约数、快速幂、取模、素数筛和组合数。

---

## 数学题先看什么

算法里的数学题通常不是考复杂定理，而是考基础模板和边界处理。

| 题目关键词 | 优先考虑 | 典型用途 |
|------------|----------|----------|
| 最大公约数、互质、约分 | `gcd` | 化简比例、判断互质、最小公倍数 |
| 幂、指数很大 | 快速幂 | `a^n`、矩阵幂、模逆元 |
| 答案很大、取余 | 模运算 | 防溢出、方案数取模 |
| 多次判断素数 | 素数筛 | 批量预处理素数 |
| 选法、路径数、概率计数 | 组合数 | `C(n, k)`、杨辉三角、预处理阶乘 |

如果数字非常大，超出普通整数范围，再考虑 [[5.算法/5.2 算法/5.2.3 高精度|高精度]]；如果本质是区间求和或计数前缀，则常和 [[5.算法/5.2 算法/5.2.4 前缀和|前缀和]] 搭配。

---

## 数学工具总览

![[图片/SVG/5_2_2_1_1.svg]]

这几类模板的共同点是：先把数学关系转成稳定的循环不变量，再控制好整数范围。

> [!warning]
> 数学题最容易错在边界：`0`、负数、`long long` 溢出、负数取模、除法取模、数组预处理范围。

---

## 最大公约数 gcd

最大公约数使用欧几里得算法：

```text
gcd(a, b) = gcd(b, a % b)
```

直到 `b == 0`，此时答案是 `a`。

```cpp
#include <cstdlib>

long long gcd(long long a, long long b) {
    a = std::llabs(a);
    b = std::llabs(b);

    while (b != 0) {
        long long r = a % b;
        a = b;
        b = r;
    }

    return a;
}
```

C++17 也可以直接使用 `<numeric>` 中的 `std::gcd`：

```cpp
#include <numeric>

int g = std::gcd(a, b);
```

### 最小公倍数 lcm

最小公倍数常用公式：

```text
lcm(a, b) = a / gcd(a, b) * b
```

写成 `a / gcd * b`，比 `a * b / gcd` 更不容易中间溢出。

```cpp
long long lcm(long long a, long long b) {
    if (a == 0 || b == 0) return 0;
    return std::llabs(a / gcd(a, b) * b);
}
```

**常见用途**：

- 判断两个数是否互质：`gcd(a, b) == 1`。
- 化简分数：分子分母同时除以 `gcd`。
- 求周期同步：两个周期第一次同时出现通常和 `lcm` 相关。

---

## 快速幂

直接计算 `a^n` 需要乘 `n` 次。快速幂利用指数的二进制，把复杂度降到 `O(log n)`。

```text
n = 13 = 1101₂
a^13 = a^8 * a^4 * a
```

```cpp
long long fastPow(long long base, long long exp) {
    long long result = 1;

    while (exp > 0) {
        if (exp & 1LL) {
            result *= base;
        }
        base *= base;
        exp >>= 1LL;
    }

    return result;
}
```

如果题目要求取模，要在每次乘法后取模：

```cpp
long long modPow(long long base, long long exp, long long mod) {
    base %= mod;
    long long result = 1 % mod;

    while (exp > 0) {
        if (exp & 1LL) {
            result = result * base % mod;
        }
        base = base * base % mod;
        exp >>= 1LL;
    }

    return result;
}
```

**关键点**：

- `exp & 1` 表示当前二进制位是否需要乘进答案。
- `base = base * base` 表示下一位对应的幂。
- 取模版每一步都 `% mod`，避免数值过大。
- 如果 `mod` 本身接近 `long long` 上限，乘法还可能溢出，需要额外的安全乘法；LeetCode 常见 `1e9+7` 场景用 `long long` 足够。

---

## 取模运算

取模题最常见的是让答案对 `1e9 + 7` 取余：

```cpp
const long long MOD = 1000000007LL;
```

加法和乘法可以直接拆开取模：

```cpp
long long addMod(long long a, long long b, long long mod) {
    return ((a % mod) + (b % mod)) % mod;
}

long long mulMod(long long a, long long b, long long mod) {
    return (a % mod) * (b % mod) % mod;
}
```

### 负数取模

C++ 中负数 `%` 的结果可能是负数，所以要规范化：

```cpp
long long normalizeMod(long long x, long long mod) {
    return (x % mod + mod) % mod;
}
```

### 除法取模

取模下不能直接写：

```cpp
// 错误直觉：不能直接把除法拆到 mod 里
answer = a / b % MOD;
```

如果 `MOD` 是质数，并且 `b` 不是 `MOD` 的倍数，可以用费马小定理求逆元：

```text
b^(MOD - 2) % MOD
```

```cpp
long long modInverse(long long x, long long mod) {
    return modPow(x, mod - 2, mod);
}
```

> [!warning]
> 模逆元不是任何时候都能用。常见力扣和笔试题给 `MOD = 1e9 + 7` 这类质数时才通常这样写。

---

## 素数筛

如果只判断一个数是否为素数，可以试除到 `sqrt(n)`；如果要批量判断 `1..n` 中的素数，埃氏筛更合适。

```cpp
#include <vector>

std::vector<int> sievePrimes(int n) {
    std::vector<bool> isPrime(n + 1, true);
    std::vector<int> primes;

    if (n >= 0) isPrime[0] = false;
    if (n >= 1) isPrime[1] = false;

    for (int i = 2; 1LL * i * i <= n; ++i) {
        if (!isPrime[i]) continue;

        for (long long j = 1LL * i * i; j <= n; j += i) {
            isPrime[static_cast<int>(j)] = false;
        }
    }

    for (int i = 2; i <= n; ++i) {
        if (isPrime[i]) {
            primes.push_back(i);
        }
    }

    return primes;
}
```

**为什么从 `i * i` 开始标记**：

- 小于 `i * i` 的 `i` 的倍数，已经被更小的因子标记过。
- 例如 `2 * 5` 会在处理 `2` 时标记，不需要等到 `5` 再标。

**复杂度**：

- 时间复杂度约为 `O(n log log n)`。
- 空间复杂度 `O(n)`。

如果题目频繁查询“某个数是否素数”，可以保留 `isPrime` 数组，而不是只返回 `primes`。

---

## 组合数

组合数 `C(n, k)` 表示从 `n` 个元素中选 `k` 个。常见递推式是：

```text
C(n, k) = C(n - 1, k - 1) + C(n - 1, k)
```

### 杨辉三角预处理

当 `n` 不大，或者不要求特别高的查询速度时，可以用动态规划预处理。

```cpp
#include <vector>

std::vector<std::vector<long long>> buildCombination(int n) {
    std::vector<std::vector<long long>> c(n + 1, std::vector<long long>(n + 1, 0));

    for (int i = 0; i <= n; ++i) {
        c[i][0] = 1;
        c[i][i] = 1;

        for (int j = 1; j < i; ++j) {
            c[i][j] = c[i - 1][j - 1] + c[i - 1][j];
        }
    }

    return c;
}
```

这个写法和 [[5.算法/5.2 算法/5.2.19 动态规划|动态规划]] 很接近：当前状态由上一行两个状态转移而来。

### 组合数取模

如果结果很大，通常对 `MOD` 取模：

```cpp
std::vector<std::vector<long long>> buildCombinationMod(int n, long long mod) {
    std::vector<std::vector<long long>> c(n + 1, std::vector<long long>(n + 1, 0));

    for (int i = 0; i <= n; ++i) {
        c[i][0] = 1;
        c[i][i] = 1;

        for (int j = 1; j < i; ++j) {
            c[i][j] = (c[i - 1][j - 1] + c[i - 1][j]) % mod;
        }
    }

    return c;
}
```

如果 `n` 很大且查询很多，可以预处理阶乘和逆元：

```text
C(n, k) = fact[n] * invFact[k] * invFact[n - k] % MOD
```

这个方案依赖模逆元，通常要求 `MOD` 是质数。

---

## 如何选择模板

| 需求 | 推荐模板 |
|------|----------|
| 单次求最大公约数 | `std::gcd` 或欧几里得算法 |
| 多次求幂且指数很大 | 快速幂 |
| 幂运算答案取模 | 模快速幂 |
| 批量判断素数 | 埃氏筛 |
| `n` 不大求组合数 | 杨辉三角 DP |
| `n` 大且多次查询组合数 | 阶乘 + 逆元 |
| 数字位数超过整数范围 | 高精度 |

面试里优先写最小可用模板，不要过早上复杂预处理。笔试里如果查询次数很多，再用预处理换时间。

---

## 常见误区

### 1. `lcm` 先乘后除导致溢出

优先写：

```cpp
a / gcd(a, b) * b
```

不要先写 `a * b / gcd(a, b)`。

### 2. 快速幂忘记更新 base

每轮都要执行：

```cpp
base = base * base;
exp >>= 1;
```

否则只是在重复乘同一个底数。

### 3. 取模下误用除法

模运算里不能直接除。需要确认是否能求逆元，或者换成不需要除法的推导。

### 4. 负数取模没有规范化

如果表达式可能变成负数，最终要写：

```cpp
(x % MOD + MOD) % MOD
```

### 5. 素数筛从 `2 * i` 开始标记

这样虽然正确，但会重复标记很多次。从 `i * i` 开始更合适。

### 6. 组合数数组范围开小

如果需要 `C(n, k)`，二维数组至少要支持下标 `[0..n][0..n]`，并处理 `k < 0` 或 `k > n` 的非法查询。

---

## 面试手写检查清单

| 检查项 | 自查问题 |
|--------|----------|
| 整数范围 | 是否需要 `long long`？乘法是否会溢出？ |
| 取模 | 是否每步都取模？负数是否规范化？ |
| gcd | 是否处理了 `0` 和负数？ |
| 快速幂 | 指数是否可能为 0？ |
| 素数筛 | `0` 和 `1` 是否标记为非素数？ |
| 组合数 | `C(n,0)` 和 `C(n,n)` 是否初始化为 1？ |
| 模逆元 | 是否确认 `MOD` 是质数且分母不为 0？ |

---

## 关键要点

1. `gcd` 用欧几里得算法，`lcm` 先除后乘。
2. 快速幂把指数按二进制拆分，复杂度是 `O(log n)`。
3. 模运算下加法、乘法能拆，除法要用逆元且有前提。
4. 埃氏筛适合批量判断素数，从 `i * i` 开始标记。
5. 组合数小范围用杨辉三角，大范围多查询用阶乘和逆元。
6. 数学模板最重要的是边界和溢出，不是代码长度。

---

## 相关链接

- [[5.算法/5.2 算法/5.2.3 高精度|高精度]] - 超出整数范围的大数计算
- [[5.算法/5.2 算法/5.2.4 前缀和|前缀和]] - 数学预处理和区间求和思想
- [[5.算法/5.2 算法/5.2.19 动态规划|动态规划]] - 组合数递推和计数 DP 的基础
- [[5.算法/5.2 算法/5.2.8 哈希表与计数|哈希表与计数]] - 计数题中常和数学性质结合
- [[5.算法/5.1 STL/5.1.11 算法|算法]] - 常用 STL 算法工具
