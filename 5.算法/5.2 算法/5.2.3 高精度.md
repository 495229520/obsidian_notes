---
tags:
  - 算法
  - STL
阅读次数: 0
---

# 5.2.3 高精度

## 什么是高精度

当整数大到连 `long long` 都装不下时（`long long` 上限约 `9.2 × 10¹⁸`，即 19 位），就无法用内置类型直接运算，只能像**手算竖式**那样，用数组逐位模拟加减乘除。

| 语言 | 大整数支持 | 是否需手写高精度 |
|------|-----------|------------------|
| Python | 原生 `int` 任意精度 | 不需要 |
| Java | `BigInteger` / `BigDecimal` | 不需要 |
| C++ | 内置类型最多 64 位 | **需要手写** |

> [!tip] 四种运算的核心套路
> | 运算 | 关键变量 | 方向 | 易错点 |
> |------|----------|------|--------|
> | 加法 | 进位 `t` | 低 → 高 | 末尾进位要补一位 |
> | 减法 | 借位 `t` | 低 → 高 | `(t+10)%10`、去前导零 |
> | 乘法（×小数） | 进位 `t` | 低 → 高 | `t += A[i]*b` 再 `%10` |
> | 除法（÷小数） | 余数 `r` | **高 → 低** | 结果需 `reverse` |

---

## 大整数的存储方式

### 存储原则

- 以字符串形式读入大整数
- **逆序存储**到 [[5.1.1 vector|vector]] 中：低位存在数组低索引，高位存在数组高索引
- 原因：进位永远朝最高位走；逆序后最高位在数组末尾，`C.push_back()` 直接追加新位即可，无需移动元素

### 图解

![[图片/SVG/5_2_2_3_1.svg]]

```cpp
string a = "12345";
// 逆序存储为: A[0]=5, A[1]=4, A[2]=3, A[3]=2, A[4]=1
for (int i = a.size() - 1; i >= 0; i--) A.push_back(a[i] - '0');
```

---

## 高精度加法

### 思路

1. 逆序存储两个大整数 A 和 B
2. 从低位开始逐位相加：`t = A[i] + B[i] + 进位`
3. 当前结果位 `= t % 10`，新进位 `= t / 10`
4. 循环结束后若进位非 0，再追加一位

### 图解

![[图片/SVG/5_2_2_3_2.svg]]

### 代码模板

```cpp
#define _CRT_SECURE_NO_WARNINGS
#include <iostream>
#include <vector>

using namespace std;

// A、B 均为逆序存储的大数，返回逆序的和 C
vector<int> add(vector<int>& A, vector<int>& B) {
    vector<int> C;
    int t = 0;  // 进位

    for (int i = 0; i < A.size() || i < B.size(); i++) {
        if (i < A.size()) t += A[i];
        if (i < B.size()) t += B[i];
        C.push_back(t % 10);  // 结果位
        t /= 10;              // 新进位
    }

    if (t) C.push_back(t);    // 末尾进位补一位（t 此时为 0 或 1）

    return C;
}

int main() {
    string a, b;
    vector<int> A, B;

    cin >> a >> b;
    // 反转存储：字符串高位在前，逆序后低位在前
    for (int i = a.size() - 1; i >= 0; i--) A.push_back(a[i] - '0');
    for (int i = b.size() - 1; i >= 0; i--) B.push_back(b[i] - '0');

    auto C = add(A, B);

    // 结果也是逆序存储，需逆序输出
    for (int i = C.size() - 1; i >= 0; i--) printf("%d", C[i]);

    return 0;
}
```

> 原模板末尾写的是 `if (t) C.push_back(1);`——这里进位恒为 0 或 1，写 `1` 没问题；但更通用的写法是 `C.push_back(t)`，对乘法等进位可能 >1 的场景同样成立。

---

## 高精度减法

### 比较大小

减法要求**大数减小数**，否则结果为负需另外加符号。先比较 A、B：

```cpp
// A >= B 返回 true
bool cmp(vector<int>& A, vector<int>& B) {
    if (A.size() != B.size()) return A.size() > B.size();  // 位数多者大
    for (int i = A.size() - 1; i >= 0; i--)                // 从最高位比
        if (A[i] != B[i]) return A[i] > B[i];
    return true;  // 完全相等也视为 A >= B
}
```

### 图解

![[图片/SVG/5_2_2_3_3.svg]]

### 代码模板

```cpp
// 保证 A >= B，返回 A - B（逆序）
vector<int> sub(vector<int>& A, vector<int>& B) {
    vector<int> C;
    int t = 0;  // 借位

    for (int i = 0; i < A.size(); i++) {
        t = A[i] - t;                  // 先扣掉上一位的借位
        if (i < B.size()) t -= B[i];
        C.push_back((t + 10) % 10);    // t 为负时 +10 取个位，否则原样
        t = t < 0 ? 1 : 0;             // 借位标记
    }

    while (C.size() > 1 && C.back() == 0) C.pop_back();  // 去前导零

    return C;
}
```

> [!note] `(t + 10) % 10` 的妙处
> - 若 `t >= 0`：`(t+10)%10 == t`，正常取这一位；
> - 若 `t < 0`（如 `-5`）：`(-5+10)%10 == 5`，等价于向高位借 1 个 10。
> 一行同时覆盖「够减」和「不够减」两种情况。

---

## 高精度乘法（大数 × 小数）

乘法与加法是**同一套进位机制**：把「逐位相加」换成「逐位乘 b 再累加进位」即可。

`t = A[i] * b + 进位`，结果位 `= t % 10`，新进位 `= t / 10`。注意此时进位可能远大于 1（如 `9*9=81`），所以循环条件要带上 `t`。

### 代码模板

```cpp
// A 为逆序大数，b 为小整数，返回 A * b（逆序）
vector<int> mul(vector<int>& A, int b) {
    vector<int> C;
    int t = 0;

    for (int i = 0; i < A.size() || t; i++) {  // t 非 0 时继续处理高位进位
        if (i < A.size()) t += A[i] * b;
        C.push_back(t % 10);
        t /= 10;
    }

    while (C.size() > 1 && C.back() == 0) C.pop_back();  // 处理 b == 0

    return C;
}
```

> 大数 × 大数则需双重循环：`num1[i] * num2[j]` 累加到结果的第 `i + j` 位（详见练习题 43）。

---

## 高精度除法（大数 ÷ 小数）

### 思路

除法是唯一**从高位开始**的运算。维护一个余数 `r`，自最高位向低位扫描：

`r = r * 10 + A[i]`，商位 `= r / b`，新余数 `= r % b`。

由于商是高位→低位生成的，与逆序存储相反，所以最后要 `reverse` 再去前导零。

### 图解

![[图片/SVG/5_2_2_3_4.svg]]

### 代码模板

```cpp
// A 为逆序大数，b 为小整数，r 引用返回余数，返回商（逆序）
vector<int> div(vector<int>& A, int b, int& r) {
    vector<int> C;
    r = 0;

    for (int i = A.size() - 1; i >= 0; i--) {  // 从最高位开始
        r = r * 10 + A[i];
        C.push_back(r / b);  // 商位（暂为高→低顺序）
        r %= b;
    }

    reverse(C.begin(), C.end());  // 翻成低→高，与其他运算对齐
    while (C.size() > 1 && C.back() == 0) C.pop_back();  // 去前导零

    return C;
}

int main() {
    string a;
    int b;

    cin >> a >> b;
    vector<int> A;
    // 除法本可不逆序，但为与其他运算保持一致仍逆序存储
    for (int i = a.size() - 1; i >= 0; i--) A.push_back(a[i] - '0');

    int r;
    auto C = div(A, b, r);

    for (int i = C.size() - 1; i >= 0; i--) printf("%d", C[i]);
    printf("\n%d", r);  // 输出余数

    return 0;
}
```

---

## 关键要点

1. **逆序存储**：低位存低索引，进位时末尾追加，免去元素搬移。
2. **进位 / 借位**：用变量 `t` 统一处理，加法 `t/10`、乘法 `t/=10`、减法看 `t<0`。
3. **去除前导零**：`while (C.size() > 1 && C.back() == 0) C.pop_back();`，保留至少一位。
4. **比较大小**：先比位数，再从最高位逐位比较。
5. **除法特殊**：从高位开始，结果需 `reverse`。

---

## 易错点

| 易错点 | 说明 |
|--------|------|
| 前导零未去除 | 减法、乘 0、除法都可能产生高位多余的 0，须 `pop_back` 但**至少保留一位**。 |
| 减法未保证 A≥B | 不先 `cmp` 就相减会得到错误的负数结果；需要负号时应交换并补 `-`。 |
| 进位写死为 1 | 加法进位是 0/1，但乘法进位可 >1，循环条件务必带 `|| t`，补位用 `push_back(t)`。 |
| 除法忘记 reverse | 商是高→低生成的，不翻转会整体倒序输出。 |
| 输出顺序 | 内部逆序存储，输出时必须从 `C.size()-1` 倒着打印。 |
| 越界访问 | 加减法两数长度不同，访问 `A[i]`/`B[i]` 前先判 `i < size()`。 |

---

## 截图题目练习分配

| 题目 | 对应模式 | 备注 | 引用 | 学习次数 |
| ---------- | ----- | ---------------------------------------------------- | ------------------------------------------------------------- | ---- |
| 415. 字符串相加 | 高精度加法 | 从低位到高位逐位相加并处理进位 | [[5.算法/5.3 力扣/5.3.3 高精度/415. 字符串相加\|415. 字符串相加]] | 0 |
| 43. 字符串相乘 | 高精度乘法 | 大数 × 大数：`num1[i]×num2[j]` 落在 `res[i+j+1]`（个位）与 `res[i+j]`（进位） | [[5.算法/5.3 力扣/5.3.3 高精度/43. 字符串相乘\|43. 字符串相乘]] | 0 |
| 67. 二进制求和 | 高精度加法（二进制） | 同样的进位模拟，`%2`、`/2` 代替 `%10`、`/10` | [[5.算法/5.3 力扣/5.3.3 高精度/67. 二进制求和\|67. 二进制求和]] | 0 |
| 989. 数组形式的整数加法 | 高精度加法 | 数组形式大数加一个 int，从末位起进位 | [[5.算法/5.3 力扣/5.3.3 高精度/989. 数组形式的整数加法\|989. 数组形式的整数加法]] | 0 |
| 66. 加一 | 进位模拟 | 加法的退化版，只需处理一次进位链 | [[5.算法/5.3 力扣/5.3.3 高精度/66. 加一\|66. 加一]] | 0 |
| 2. 两数相加 | 链表高精度加法 | 链表天然逆序存储，边遍历边进位 | [[5.算法/5.3 力扣/5.3.3 高精度/2. 两数相加\|2. 两数相加]] | 0 |

## 相关链接

- [[5.1.1 vector]] - vector 容器用于存储大整数
- [[5.1.6 string]] - 字符串用于读入大整数
