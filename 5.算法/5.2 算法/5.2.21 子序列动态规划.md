---
tags:
  - 算法/动态规划
  - 算法/子序列
  - 算法
  - STL
阅读次数: 0
---

# 5.2.21 子序列动态规划

> 子序列 DP 处理的是“不要求连续，但要求保持相对顺序”的问题。LIS、LCS、编辑距离、最长回文子序列都属于 LeetCode 高频模型。

---

## 子序列 DP 解决什么问题

子序列和子数组不同：

- 子数组 / 子串：必须连续。
- 子序列：可以跳过元素，但相对顺序不能变。

| 题目信号 | 常见模型 |
|----------|----------|
| 最长递增子序列 | LIS，一维 DP 或贪心 + 二分 |
| 两个字符串最长公共部分 | LCS，二维 DP |
| 最少插入/删除/替换 | 编辑距离类 DP |
| 回文子序列 | 区间式子序列 DP |
| 判断是否为另一个串的子序列 | 双指针或 DP |

和 [[5.算法/5.2 算法/5.2.7 双指针与滑动窗口|双指针与滑动窗口]] 相比，子序列 DP 允许跳过中间元素；和 [[5.算法/5.2 算法/5.2.19 动态规划|动态规划]] 总纲相比，本篇专注“以位置结尾”和“两串前缀”两种状态定义。

![[图片/5.算法/5.2 算法/5_2_2_21_1.svg]]

---

## 一维子序列：LIS

最长递增子序列（LIS）的经典状态定义是：

```text
dp[i] = 以 nums[i] 结尾的最长递增子序列长度
```

转移：

```text
如果 j < i 且 nums[j] < nums[i]
dp[i] = max(dp[i], dp[j] + 1)
```

```cpp
#include <algorithm>
#include <vector>

int lengthOfLIS(const std::vector<int>& nums) {
    if (nums.empty()) return 0;

    std::vector<int> dp(nums.size(), 1);
    int best = 1;

    for (int i = 0; i < static_cast<int>(nums.size()); ++i) {
        for (int j = 0; j < i; ++j) {
            if (nums[j] < nums[i]) {
                dp[i] = std::max(dp[i], dp[j] + 1);
            }
        }
        best = std::max(best, dp[i]);
    }

    return best;
}
```

**为什么答案不是 `dp[n - 1]`**：

- `dp[i]` 表示必须以 `nums[i]` 结尾。
- 最长递增子序列可以在任意位置结尾。
- 因此答案是所有 `dp[i]` 的最大值。

---

## LIS 的二分优化

LIS 还有一个 `O(nlogn)` 写法，用 `tails[len]` 表示长度为 `len + 1` 的递增子序列的最小结尾值。

```cpp
#include <algorithm>
#include <vector>

int lengthOfLISBinary(const std::vector<int>& nums) {
    std::vector<int> tails;

    for (int x : nums) {
        auto it = std::lower_bound(tails.begin(), tails.end(), x);
        if (it == tails.end()) {
            tails.push_back(x);
        } else {
            *it = x;
        }
    }

    return static_cast<int>(tails.size());
}
```

> [!warning]
> `tails` 本身不一定是一条真实的 LIS，它维护的是“同长度子序列的最优结尾候选”。

如果题目要求非严格递增，把 `lower_bound` 换成 `upper_bound`。

---

## 两个序列：LCS

最长公共子序列（LCS）通常使用二维 DP：

```text
dp[i][j] = text1 前 i 个字符，与 text2 前 j 个字符的 LCS 长度
```

转移：

```text
text1[i - 1] == text2[j - 1]:
    dp[i][j] = dp[i - 1][j - 1] + 1
否则:
    dp[i][j] = max(dp[i - 1][j], dp[i][j - 1])
```

```cpp
#include <algorithm>
#include <string>
#include <vector>

int longestCommonSubsequence(const std::string& a, const std::string& b) {
    int m = static_cast<int>(a.size());
    int n = static_cast<int>(b.size());
    std::vector<std::vector<int>> dp(m + 1, std::vector<int>(n + 1, 0));

    for (int i = 1; i <= m; ++i) {
        for (int j = 1; j <= n; ++j) {
            if (a[i - 1] == b[j - 1]) {
                dp[i][j] = dp[i - 1][j - 1] + 1;
            } else {
                dp[i][j] = std::max(dp[i - 1][j], dp[i][j - 1]);
            }
        }
    }

    return dp[m][n];
}
```

**为什么数组多开一行一列**：

- `dp[0][j]` 表示空串和 `b` 前 `j` 个字符的 LCS，值为 0。
- `dp[i][0]` 表示 `a` 前 `i` 个字符和空串的 LCS，值为 0。
- 多开的边界能避免大量 `i - 1`、`j - 1` 的越界判断。

---

## 编辑距离

编辑距离问的是：把 `word1` 变成 `word2`，最少需要插入、删除、替换多少次。

```text
dp[i][j] = word1 前 i 个字符变成 word2 前 j 个字符的最少操作数
```

```cpp
#include <algorithm>
#include <string>
#include <vector>

int minDistance(const std::string& a, const std::string& b) {
    int m = static_cast<int>(a.size());
    int n = static_cast<int>(b.size());
    std::vector<std::vector<int>> dp(m + 1, std::vector<int>(n + 1, 0));

    for (int i = 0; i <= m; ++i) dp[i][0] = i;
    for (int j = 0; j <= n; ++j) dp[0][j] = j;

    for (int i = 1; i <= m; ++i) {
        for (int j = 1; j <= n; ++j) {
            if (a[i - 1] == b[j - 1]) {
                dp[i][j] = dp[i - 1][j - 1];
            } else {
                dp[i][j] = 1 + std::min({
                    dp[i - 1][j],
                    dp[i][j - 1],
                    dp[i - 1][j - 1]
                });
            }
        }
    }

    return dp[m][n];
}
```

三个来源分别表示：

| 来源 | 操作含义 |
|------|----------|
| `dp[i - 1][j]` | 删除 `a[i - 1]` |
| `dp[i][j - 1]` | 插入 `b[j - 1]` |
| `dp[i - 1][j - 1]` | 替换 `a[i - 1]` |

---

## 最长回文子序列

最长回文子序列只有一个字符串，但它的状态是区间：

```text
dp[i][j] = s[i..j] 中最长回文子序列长度
```

```cpp
#include <algorithm>
#include <string>
#include <vector>

int longestPalindromeSubseq(const std::string& s) {
    int n = static_cast<int>(s.size());
    std::vector<std::vector<int>> dp(n, std::vector<int>(n, 0));

    for (int i = n - 1; i >= 0; --i) {
        dp[i][i] = 1;
        for (int j = i + 1; j < n; ++j) {
            if (s[i] == s[j]) {
                dp[i][j] = dp[i + 1][j - 1] + 2;
            } else {
                dp[i][j] = std::max(dp[i + 1][j], dp[i][j - 1]);
            }
        }
    }

    return n == 0 ? 0 : dp[0][n - 1];
}
```

这里 `i` 必须倒序，因为 `dp[i][j]` 依赖 `dp[i + 1][j]`。

---

## 如何识别题型

| 题目特征 | 推荐状态 |
|----------|----------|
| 单数组，要求递增/递减/整除链 | `dp[i] = 以 i 结尾的最优解` |
| 两个字符串/数组，保持相对顺序 | `dp[i][j] = 两个前缀的最优解` |
| 允许插入、删除、替换 | 编辑距离状态 |
| 一个字符串内部找回文子序列 | `dp[i][j] = 区间内最优解` |
| 要求连续子串/子数组 | 不优先用子序列模型 |

---

## 复杂度总结

| 模型 | 时间复杂度 | 空间复杂度 |
|------|------------|------------|
| LIS 二重循环 | `O(n²)` | `O(n)` |
| LIS 二分优化 | `O(nlogn)` | `O(n)` |
| LCS | `O(mn)` | `O(mn)` |
| 编辑距离 | `O(mn)` | `O(mn)` |
| 回文子序列 | `O(n²)` | `O(n²)` |

---

## 常见误区

### 1. 混淆子序列和子串

子序列可以跳过元素，子串必须连续。最长回文子串和最长回文子序列不是同一个问题。

### 2. LIS 答案误取最后一个状态

`dp[i]` 是“以 i 结尾”，答案要取全局最大值。

### 3. LCS 下标错位

`dp[i][j]` 表示前 `i`、前 `j` 个字符，所以比较的是 `a[i - 1]` 和 `b[j - 1]`。

### 4. 回文子序列遍历方向错误

`dp[i][j]` 依赖 `i + 1`，所以 `i` 要倒序。

---

## 关键要点

1. 子序列不要求连续，但必须保持相对顺序。
2. LIS 常用“以 i 结尾”的一维状态。
3. LCS 常用“两个前缀”的二维状态。
4. 编辑距离的插入、删除、替换都来自相邻子问题。
5. 最长回文子序列本质上是区间 DP。

---

## 截图题目练习分配

| 题目 | 对应模式 | 备注 | CodeTop热度 | CodeTop频率 |
| --- | --- | --- | --- | --- |
| 300. 最长上升子序列 | LIS | `dp[i]` 或贪心 + 二分优化 | hot100 | 267 |
| 1143. 最长公共子序列 | 双序列 DP | `dp[i][j]` 表示两个前缀的 LCS 长度 | hot100 | 196 |
| 128. 最长连续序列 | 哈希集合 / 序列起点 | 只从连续序列起点开始扩展 | hot100 | 92 |
| 718. 最长重复子数组 | 连续子数组 DP | `dp[i][j]` 表示以两位置结尾的公共后缀长度 | hot100 | 68 |
| 72. 编辑距离 | 双序列 DP | 插入、删除、替换三种转移 | hot100 | 200 |
| 560. 和为 K 的子数组 | 前缀和 + 哈希 | 用 `prefix - k` 计数历史前缀 | hot100 | 78 |
| 209. 长度最小的子数组 | 滑动窗口 / 前缀和 | 正数数组可用窗口收缩 | hot100 | 73 |
| 152. 乘积最大子数组 | 线性 DP | 同时维护最大/最小乘积 | hot100 | 85 |
| 516. 最长回文子序列 | 区间 / 子序列 DP | 两端字符相等时向内转移 | hot300 | 37 |
| 673. 最长递增子序列的个数 | LIS 计数 DP | 同时维护长度和方案数 | hot300 | 28 |
| 1044. 最长重复子串 | 后缀/二分 + 哈希 | 二分长度并判断是否存在重复子串 | hot300 | 22 |
| 115. 不同的子序列 | 双序列 DP | 统计 `s` 的子序列匹配 `t` 的方案数 | hot300 | 19 |
| 410. 分割数组的最大值 | 二分答案 / DP | 最小化最大子数组和 | hot300 | 19 |
| 剑指 Offer 48. 最长不含重复字符的子字符串 | 滑动窗口 / 子序列对比 | 维护无重复连续窗口 | hot300 | 18 |
| 674. 最长连续递增序列 | 线性 DP | 当前连续递增长度由前一位转移 | hot300 | 17 |
| 862. 和至少为 K 的最短子数组 | 前缀和 + 单调队列 | 维护递增前缀和下标队列 | hot300 | 16 |
| 525. 连续数组 | 前缀和 + 哈希 | 把 0 当作 -1，找相同前缀差 | hot300 | 16 |
| 523. 连续的子数组和 | 前缀和取模 | 相同余数形成可整除区间 | hot300 | 15 |
| 994. 腐烂的橘子 | BFS 层序 | 多源 BFS 计算扩散轮数 | hot300 | 15 |
| 540. 有序数组中的单一元素 | 二分 / 奇偶下标 | 利用成对元素位置规律 | hot300 | 15 |
| 873. 最长的斐波那契子序列的长度 | 子序列 DP + 哈希 | `dp[j][i]` 表示以两个数结尾的长度 | 低频 | 6 |
| 376. 摆动序列 | 贪心 / DP | 维护上升和下降结尾长度 | 低频 | 6 |
| 413. 等差数列划分 | 线性 DP | 统计以当前位置结尾的等差子数组数量 | 低频 | 2 |
| 1004. 最大连续 1 的个数 III | 滑动窗口 | 窗口内最多允许 `k` 个 0 | hot300 | 28 |

## 相关链接

- [[5.算法/5.2 算法/5.2.19 动态规划|动态规划]] - DP 总纲与五步法
- [[5.算法/5.3 力扣/动态规划/5. 最长回文子串|5. 最长回文子串]] - 注意区分子串和子序列
- [[53. 最大子数组和|53. 最大子数组和]] - 连续子数组 DP 对比
- [[5.算法/5.1 STL/5.1.1 vector|vector]] - DP 表存储
- [[5.算法/5.1 STL/5.1.6 string|string]] - 字符串 DP 的基础容器
